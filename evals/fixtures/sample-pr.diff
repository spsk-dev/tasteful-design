diff --git a/src/services/user.ts b/src/services/user.ts
index 3a1b2c4..8f9e0d1 100644
--- a/src/services/user.ts
+++ b/src/services/user.ts
@@ -12,6 +12,7 @@ import { UserRepository } from '../repositories/user.repository';
 import { hashPassword, verifyPassword } from '../utils/crypto';
 import { Logger } from '../utils/logger';
 import { NotFoundError, ValidationError } from '../errors';
+import { normalizeEmail } from '../utils/validate';

 const logger = new Logger('UserService');

@@ -22,14 +23,32 @@ export class UserService {
   ) {}

   /**
-   * Find user by ID
+   * Find user by ID with optional relations
    */
-  async findById(id: string): Promise<User> {
-    const user = await this.userRepo.findOne({ where: { id } });
+  async findById(id: string, relations?: string[]): Promise<User> {
+    const user = await this.userRepo.findOne({
+      where: { id },
+      relations: relations || [],
+    });
     if (!user) {
       throw new NotFoundError(`User ${id} not found`);
     }
     return user;
   }

+  /**
+   * Update user profile fields
+   */
+  async updateProfile(
+    userId: string,
+    updates: Partial<Pick<User, 'name' | 'avatar' | 'timezone'>>
+  ): Promise<User> {
+    const user = await this.userRepo.findOne({ where: { id: userId } });
+
+    // BUG: Missing null check -- user could be undefined if deleted between auth and this call
+    user.name = updates.name ?? user.name;
+    user.avatar = updates.avatar ?? user.avatar;
+    user.timezone = updates.timezone ?? user.timezone;
+
+    return this.userRepo.save(user);
+  }
+
   /**
    * Search users by email prefix (for autocomplete)
    */
@@ -41,7 +60,7 @@ export class UserService {
       .getMany();
   }

-  async deactivate(id: string): Promise<void> {
+  async deactivate(id: string, reason?: string): Promise<void> {
     const user = await this.findById(id);
     user.isActive = false;
     user.deactivatedAt = new Date();
diff --git a/src/utils/validate.ts b/src/utils/validate.ts
new file mode 100644
index 0000000..a7c3d4e
--- /dev/null
+++ b/src/utils/validate.ts
@@ -0,0 +1,28 @@
+/**
+ * Input validation and normalization utilities.
+ */
+
+const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
+
+/**
+ * Normalize an email address: trim whitespace, lowercase, remove dots
+ * from the local part (Gmail-style normalization).
+ */
+export function normalizeEmail(email: string): string {
+  const trimmed = email.trim().toLowerCase();
+  const [local, domain] = trimmed.split('@');
+  // Remove dots from local part for canonical form
+  const normalized = local.replace(/\./g, '');
+  return `${normalized}@${domain}`;
+}
+
+/**
+ * Validate that a string is a well-formed email address.
+ */
+export function isValidEmail(email: string): boolean {
+  return EMAIL_REGEX.test(email);
+}
+
+export function sanitizeDisplayName(name: string): string {
+  return name.replace(/[<>&"']/g, '').trim().slice(0, 100);
+}
