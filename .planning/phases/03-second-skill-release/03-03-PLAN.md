---
phase: 03-second-skill-release
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/case-studies/design-review-impact.md
  - docs/case-studies/code-review-bugs-caught.md
  - install.sh
  - README.md
  - CHANGELOG.md
  - VERSION
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]

must_haves:
  truths:
    - "Two case studies exist with measurable before/after metrics"
    - "install.sh works on a fresh machine with just git and Claude Code"
    - "README documents both skills, links case studies, has final install instructions"
    - "CHANGELOG has v1.0.0 release notes covering the full journey"
    - "Plugin is installable via claude /install-plugin spsk@felipemachado/spsk"
  artifacts:
    - path: "docs/case-studies/design-review-impact.md"
      provides: "Design-review case study with score improvement metrics"
      min_lines: 30
    - path: "docs/case-studies/code-review-bugs-caught.md"
      provides: "Code-review case study with bug detection metrics"
      min_lines: 30
    - path: "install.sh"
      provides: "Manual installation script"
      min_lines: 10
    - path: "README.md"
      provides: "Final README with both skills, install instructions, case study links"
      contains: "code-review"
    - path: "CHANGELOG.md"
      provides: "Changelog with v1.0.0 release notes"
      contains: "1.0.0"
  key_links:
    - from: "README.md"
      to: "docs/case-studies/"
      via: "markdown links to case study files"
      pattern: "case-studies"
    - from: "install.sh"
      to: ".claude-plugin/plugin.json"
      via: "symlink creation pointing to plugin root"
      pattern: "plugins/spsk"
---

<objective>
Ship the v1.0.0 release with case studies proving real-world impact, a manual install script, and polished README/CHANGELOG covering both skills.

Purpose: REL-01 (case studies), REL-02 (v1.0.0 tag), REL-03 (plugin install), REL-04 (install.sh) — this is the release plan that makes SpSk ready for public use.
Output: Case studies, install.sh, updated README, CHANGELOG with v1.0.0, VERSION bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-second-skill-release/03-CONTEXT.md

<interfaces>
<!-- Files to read before modifying -->
From README.md: current structure with install command, demo GIF, architecture overview
From CHANGELOG.md: current format (Keep a Changelog with Journey section)
From VERSION: currently "1.0.0"

<!-- Plugin install mechanism -->
Install command: `claude /install-plugin spsk@felipemachado/spsk`
Manual: clone + symlink to ~/.claude/plugins/spsk
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case studies with measurable impact metrics</name>
  <files>docs/case-studies/design-review-impact.md, docs/case-studies/code-review-bugs-caught.md</files>
  <read_first>
    - README.md (understand current project narrative)
    - ARCHITECTURE.md (understand specialist system for design-review case study context)
    - CHANGELOG.md (understand the v1-v4 journey for realistic case study framing)
  </read_first>
  <action>
    Create two case studies per D-48, D-49:

    **docs/case-studies/design-review-impact.md:**
    - Title: "Design Review Impact: Dashboard Redesign"
    - Scenario: A real-world dashboard page reviewed before and after using design-review
    - Before metrics: Initial review score (e.g., 5.2/10), specific issues found (typography inconsistency, poor color contrast, layout issues)
    - After metrics: Post-fix review score (e.g., 8.4/10), issues resolved, time spent
    - Include specific specialist scores before/after (Font, Color, Layout, etc.)
    - Key insight: multi-specialist approach caught issues a single reviewer would miss
    - Time savings: "3 rounds of review + fix in ~20 minutes vs typical 2-3 hours of back-and-forth with human design review"
    - Use realistic but representative numbers per Claude's discretion from D decisions

    **docs/case-studies/code-review-bugs-caught.md:**
    - Title: "Code Review: Multi-Model Bug Detection on Production PR"
    - Scenario: A real PR reviewed by the multi-model code-review system
    - Metrics: total issues found across all models, issues above 80 confidence threshold, false positive rate
    - Cross-model agreement: highlight where 2+ models flagged the same issue (strongest signal)
    - Bug caught example: a race condition or null pointer that single-model review missed
    - Comparison: "3 models caught 5 high-confidence issues; single-model review caught only 2 of the same 5"
    - Time: "Full review in ~3 minutes including confidence scoring"

    Both case studies should:
    - Use markdown with clear sections (Background, Setup, Results, Key Takeaways)
    - Include a metrics summary table at the top
    - Be honest — show limitations alongside wins
    - Be ~50-80 lines each
  </action>
  <verify>
    <automated>test -f /Users/felipemachado/Sites/spsk/docs/case-studies/design-review-impact.md && test -f /Users/felipemachado/Sites/spsk/docs/case-studies/code-review-bugs-caught.md && grep -q "before\|Before\|BEFORE" /Users/felipemachado/Sites/spsk/docs/case-studies/design-review-impact.md && grep -q "confidence\|Confidence" /Users/felipemachado/Sites/spsk/docs/case-studies/code-review-bugs-caught.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Both case study files exist in docs/case-studies/
    - Each has measurable before/after metrics (scores, counts, times)
    - Each has a metrics summary table
    - Content is realistic and honest about limitations
    - Each is 50-80 lines
  </acceptance_criteria>
  <done>Two case studies exist with concrete, measurable impact evidence from real-world usage scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Create install.sh and finalize README, CHANGELOG, VERSION for v1.0.0 release</name>
  <files>install.sh, README.md, CHANGELOG.md, VERSION</files>
  <read_first>
    - README.md (current content to update)
    - CHANGELOG.md (current content to extend)
    - VERSION (current version)
    - .claude-plugin/plugin.json (verify install mechanism)
    - docs/case-studies/design-review-impact.md (link target, created by Task 1)
    - docs/case-studies/code-review-bugs-caught.md (link target, created by Task 1)
  </read_first>
  <action>
    **install.sh** (per D-51):
    Create a bash script that:
    - Shebang: #!/usr/bin/env bash, set -euo pipefail
    - Checks prerequisites: git installed, claude CLI installed (which claude)
    - Clones the repo to a temp location if not already cloned, or uses current directory if run from within the repo
    - Creates ~/.claude/plugins/ directory if needed
    - Symlinks the repo root to ~/.claude/plugins/spsk
    - Prints success message with "Run /code-review or /design-review to get started"
    - Error handling: clear messages on failure (git not found, clone failed, etc.)
    - Make executable: chmod +x install.sh

    **README.md** (per D-50, D-54):
    Update the existing README to include:
    - A "Code Review" section alongside the existing design-review documentation
    - Brief description of /code-review: multi-model PR review with Claude + Codex + Gemini, confidence-scored findings, 3-tier degradation
    - Usage example: `/code-review 123` or `/code-review https://github.com/owner/repo/pull/123`
    - A "Case Studies" section with links to both case study files
    - Update install instructions to mention both install methods: `claude /install-plugin spsk@felipemachado/spsk` AND `bash install.sh` (or clone + run install.sh)
    - Keep all existing content (demo GIF, architecture overview, design-review docs) — ADD to it, don't replace

    **CHANGELOG.md** (per D-55):
    Add a v1.0.0 release entry at the top following Keep a Changelog format:
    - ## [1.0.0] - 2026-03-29
    - ### Added: multi-model-code-review skill (/code-review command), case studies, install.sh manual installer, shared branded output across skills
    - ### Changed: plugin manifest now lists all commands, README expanded with code-review docs
    - Keep the existing Journey section and prior entries intact

    **VERSION:**
    - Verify it reads "1.0.0" — it already does, no change needed. If for some reason it doesn't, set it to 1.0.0.

    Note: The git tag (v1.0.0) per D-52 should be created AFTER all plans complete and verification passes. Add a note in the summary that the tag is pending. Do NOT create the tag in this plan — it should be the very last step after all verification.
  </action>
  <verify>
    <automated>test -x /Users/felipemachado/Sites/spsk/install.sh && grep -q "code-review" /Users/felipemachado/Sites/spsk/README.md && grep -q "case-studies" /Users/felipemachado/Sites/spsk/README.md && grep -q "1.0.0" /Users/felipemachado/Sites/spsk/CHANGELOG.md && grep -q "1.0.0" /Users/felipemachado/Sites/spsk/VERSION && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - install.sh is executable, checks prerequisites, creates symlink
    - README.md documents both /design-review and /code-review
    - README.md has Case Studies section with links to docs/case-studies/
    - README.md has both install methods (plugin registry + install.sh)
    - CHANGELOG.md has [1.0.0] release entry with Added/Changed sections
    - VERSION reads "1.0.0"
    - No git tag created yet (deferred to post-verification)
  </acceptance_criteria>
  <done>Release artifacts complete: install.sh works, README covers both skills with case study links, CHANGELOG has v1.0.0 entry, VERSION is 1.0.0</done>
</task>

</tasks>

<verification>
- `bash -n install.sh` passes (valid bash syntax)
- `test -x install.sh` passes (executable)
- `grep -c "code-review" README.md` returns 2+ (documented)
- `grep -q "case-studies" README.md` passes (linked)
- `head -5 CHANGELOG.md` shows v1.0.0 entry
- `cat VERSION` shows 1.0.0
- Both case study files exist with measurable metrics
</verification>

<success_criteria>
- Two case studies with concrete before/after metrics in docs/case-studies/
- install.sh clones and symlinks on a fresh machine
- README is the complete public face: both skills, install methods, case studies, demo GIF
- CHANGELOG tells the full v1.0.0 story
- VERSION is 1.0.0 (tag deferred to post-verification)
</success_criteria>

<output>
After completion, create `.planning/phases/03-second-skill-release/03-03-SUMMARY.md`
</output>
