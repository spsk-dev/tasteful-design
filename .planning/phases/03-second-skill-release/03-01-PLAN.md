---
phase: 03-second-skill-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/code-review.md
  - skills/code-review/SKILL.md
  - skills/code-review/references/review-guidelines.md
  - .claude-plugin/plugin.json
autonomous: true
requirements: [CREV-01, CREV-02]
must_haves:
  truths:
    - "A /code-review command exists and appears in plugin help"
    - "The command orchestrates Claude + Codex + Gemini in parallel with confidence-scored findings"
    - "Output uses shared branded format from shared/output.md"
    - "3-tier degradation works: Tier 1 (all 3 models), Tier 2 (2 models), Tier 3 (Claude-only)"
  artifacts:
    - path: "commands/code-review.md"
      provides: "Code review slash command with YAML frontmatter and branded output"
      min_lines: 80
    - path: "skills/code-review/SKILL.md"
      provides: "Skill descriptor for code-review"
      min_lines: 10
    - path: "skills/code-review/references/review-guidelines.md"
      provides: "Review guidelines reference file"
      min_lines: 20
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with code-review command registered"
      contains: "code-review"
  key_links:
    - from: "commands/code-review.md"
      to: "shared/output.md"
      via: "shared output reference"
      pattern: "shared/output.md"
    - from: "commands/code-review.md"
      to: "skills/code-review/references/review-guidelines.md"
      via: "plugin root path reference"
      pattern: "review-guidelines.md"
---

<objective>
Port the multi-model-code-review skill into SpSk as the second plugin command, proving SpSk is a multi-skill platform.

Purpose: CREV-01 (code-review skill) and CREV-02 (shared branded output) -- the skill must use the same branding patterns as design-review, proving shared output works across skills.
Output: Working /code-review command with skill files, review guidelines reference, and updated plugin manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-second-skill-release/03-CONTEXT.md

<interfaces>
<!-- Source skill to port (2 files, 163 lines total) -->
Source: ~/.claude/skills/multi-model-code-review/SKILL.md
Source: ~/.claude/skills/multi-model-code-review/review-guidelines.md

<!-- Existing patterns to follow -->
From commands/design-review.md, the frontmatter pattern uses YAML with fields: name, description (multiline >), allowed-tools. See the actual file for exact format.

From shared/output.md (branding contract):
- Signature line format: SpSk  {command}  v{version}  (unicode dash)  {details}
- Score bars, symbol vocabulary, section headers, verdict box, footer
- Reference via: @${CLAUDE_PLUGIN_ROOT}/shared/output.md

From .claude-plugin/plugin.json (current manifest): has name, version, description, author, homepage, license fields. No commands array yet.

From skills/design-review/ (directory structure pattern):
- skills/design-review/SKILL.md
- skills/design-review/references/*.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port code-review command and skill files</name>
  <files>commands/code-review.md, skills/code-review/SKILL.md, skills/code-review/references/review-guidelines.md</files>
  <read_first>
    - ~/.claude/skills/multi-model-code-review/SKILL.md (source to port)
    - ~/.claude/skills/multi-model-code-review/review-guidelines.md (source reference)
    - commands/design-review.md (pattern to follow for frontmatter and branding)
    - shared/output.md (branding contract to reference)
    - skills/design-review/SKILL.md (pattern for skill descriptor)
  </read_first>
  <action>
    Port the multi-model-code-review skill into SpSk as a command + skill structure:

    **commands/code-review.md** (per D-37, D-40, D-39, D-43):
    - YAML frontmatter: name "code-review", description referencing multi-model PR review, allowed-tools including Bash(gh *), Bash(codex *), Bash(gemini *), Bash(which *), Bash(cd *), Bash(cp *), Bash(rm *)
    - First line after frontmatter: @${CLAUDE_PLUGIN_ROOT}/shared/output.md
    - Add an "Output Format" section (same as design-review.md has) telling the command to use branded output. Adapt for code-review: signature line uses "code-review" as command, agent count instead of specialist count, tier from CLI availability
    - Port the full SKILL.md content but replace ALL hardcoded paths:
      - ~/.claude/skills/multi-model-code-review/review-guidelines.md becomes ${CLAUDE_PLUGIN_ROOT}/skills/code-review/references/review-guidelines.md
      - Any other absolute paths become ${CLAUDE_PLUGIN_ROOT}-relative
    - Adapt the comment format footer to include SpSk branding: replace the plain "Generated with Claude Code + Codex + Gemini" with the SpSk footer format
    - Keep all 7-agent orchestration logic, pre-flight checks, false positive guidance, confidence scoring verbatim from source
    - Degradation tiers per D-39: Tier 1 (Claude + Codex + Gemini), Tier 2 (Claude + one CLI), Tier 3 (Claude-only). Report tier in output signature line

    **skills/code-review/SKILL.md** (per D-38):
    - Minimal skill descriptor with frontmatter (name, description)
    - Point to command: "Use /code-review to invoke"
    - Brief capability summary

    **skills/code-review/references/review-guidelines.md** (per D-38, D-43):
    - Copy from source ~/.claude/skills/multi-model-code-review/review-guidelines.md
    - No path changes needed (content is self-contained)
  </action>
  <verify>
    <automated>grep -q "^name: code-review" commands/code-review.md && grep -q "shared/output.md" commands/code-review.md && test -f skills/code-review/SKILL.md && test -f skills/code-review/references/review-guidelines.md && ! grep -r "/Users/" commands/code-review.md skills/code-review/ && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - commands/code-review.md exists with valid YAML frontmatter (name, description, allowed-tools)
    - References shared/output.md via @${CLAUDE_PLUGIN_ROOT}/shared/output.md
    - All 7 agents described with orchestration logic
    - 3-tier degradation documented
    - No hardcoded absolute paths -- all use ${CLAUDE_PLUGIN_ROOT}
    - skills/code-review/SKILL.md exists with frontmatter
    - skills/code-review/references/review-guidelines.md exists with review guidelines content
  </acceptance_criteria>
  <done>The /code-review command file is complete with branded output, all agent orchestration, degradation tiers, and skill files in the correct directory structure</done>
</task>

<task type="auto">
  <name>Task 2: Register code-review in plugin manifest</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json (current manifest)
  </read_first>
  <action>
    **plugin.json** (per D-41):
    - Add a "commands" array to plugin.json: "commands": ["design", "design-review", "design-improve", "design-validate", "design-init", "code-review"]
    - Keep all existing fields (name, version, description, author, homepage, license)
    - Update description to reflect both skills: "AI-powered design review and multi-model code review -- 8 specialist agents for design, 3-model consensus for code"

    **Note:** code-review is a standalone top-level command (/code-review), NOT routed through /design. Do NOT modify commands/design.md.
  </action>
  <verify>
    <automated>jq -e '.commands | index("code-review")' .claude-plugin/plugin.json && jq empty .claude-plugin/plugin.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - plugin.json contains a "commands" array listing all 6 commands including "code-review"
    - plugin.json remains valid JSON
    - Description updated to cover both skills
  </acceptance_criteria>
  <done>Plugin manifest registers code-review as a command discoverable by the plugin registry</done>
</task>

</tasks>

<verification>
- All new files exist: commands/code-review.md, skills/code-review/SKILL.md, skills/code-review/references/review-guidelines.md
- plugin.json lists code-review in commands array
- No hardcoded paths in any new file: grep -rn "/Users/" commands/code-review.md skills/code-review/
- Branded output referenced: grep "shared/output.md" commands/code-review.md
- 3-tier degradation present: grep -c "Tier" commands/code-review.md returns 3+
</verification>

<success_criteria>
- /code-review command file exists with full 7-agent orchestration, confidence scoring, and branded output
- Skill files mirror the skills/design-review/ directory pattern
- Plugin manifest updated with code-review command
- All paths use ${CLAUDE_PLUGIN_ROOT}, zero hardcoded paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-second-skill-release/03-01-SUMMARY.md`
</output>
