---
phase: 03-second-skill-release
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - evals/validate-structure.sh
  - evals/assertions-code-review.json
  - evals/fixtures/sample-pr.diff
  - evals/run-evals.sh
autonomous: true
requirements: [CREV-03]

must_haves:
  truths:
    - "Structural validator checks code-review files exist and are properly formed"
    - "A sample PR diff fixture exists for quality eval assertions"
    - "Range-based quality assertions exist for code-review evaluation"
    - "run-evals.sh includes code-review in its orchestration"
  artifacts:
    - path: "evals/validate-structure.sh"
      provides: "Extended structural validator with Phase 3 code-review checks"
      contains: "code-review"
    - path: "evals/assertions-code-review.json"
      provides: "Range-based quality assertions for code-review evals"
      min_lines: 5
    - path: "evals/fixtures/sample-pr.diff"
      provides: "Anonymized sample PR diff for eval testing"
      min_lines: 20
  key_links:
    - from: "evals/validate-structure.sh"
      to: "commands/code-review.md"
      via: "file existence and frontmatter checks"
      pattern: "code-review"
    - from: "evals/validate-structure.sh"
      to: "skills/code-review/SKILL.md"
      via: "file existence check"
      pattern: "skills/code-review"
---

<objective>
Build the independent eval harness for the code-review skill, extending the existing two-layer eval system with code-review-specific structural checks and quality assertions.

Purpose: CREV-03 — the code-review skill needs its own eval proof, same standard as design-review.
Output: Extended structural validator, code-review assertions file, sample diff fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-second-skill-release/03-CONTEXT.md

<interfaces>
<!-- Existing eval patterns to extend -->
From evals/validate-structure.sh:
- Uses check() function: `check "label" command`
- PASS/FAIL counters, exit 1 on failures
- Phase sections separated by comments

From evals/assertions.json (design-review pattern):
```json
[
  { "fixture": "...", "dimension": "...", "min": N, "max": N, "description": "..." }
]
```

From evals/run-evals.sh:
- Orchestrates Layer 1 (structural) + Layer 2 (quality)
- Layer 2 currently stubbed for design-review

<!-- Files created by Plan 01 that we validate -->
- commands/code-review.md (must exist, have frontmatter)
- skills/code-review/SKILL.md (must exist, have frontmatter)
- skills/code-review/references/review-guidelines.md (must exist)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend structural validator and create eval fixtures</name>
  <files>evals/validate-structure.sh, evals/assertions-code-review.json, evals/fixtures/sample-pr.diff</files>
  <read_first>
    - evals/validate-structure.sh (existing validator to extend)
    - evals/assertions.json (pattern for assertions file)
    - evals/run-evals.sh (orchestrator to understand integration point)
  </read_first>
  <action>
    **evals/validate-structure.sh** (per D-44, D-46):
    Add a "Phase 3: Second skill + release" section after the Phase 2 section with these checks:
    - commands/code-review.md exists
    - commands/code-review.md has frontmatter (head -1 grep '^---')
    - commands/code-review.md has description field
    - commands/code-review.md references shared/output.md
    - skills/code-review/SKILL.md exists
    - skills/code-review/SKILL.md has frontmatter
    - skills/code-review/references/review-guidelines.md exists
    - plugin.json commands array includes "code-review"
    - evals/assertions-code-review.json is valid JSON
    - evals/fixtures/sample-pr.diff exists
    - install.sh exists and is executable (will be created by Plan 03 — use SKIP pattern if missing, same as ARCHITECTURE.md pattern in Phase 1)
    - CHANGELOG.md contains "1.0.0" (will be updated by Plan 03 — use check, it already exists)

    **evals/assertions-code-review.json** (per D-45, D-47):
    Create range-based assertions following the same schema as assertions.json. Include assertions for:
    - Overall confidence score range (fixture: sample-pr.diff, dimension: overall, min: 0, max: 100)
    - Issue count range (fixture: sample-pr.diff, dimension: issue_count, min: 0, max: 20)
    - Severity distribution (at least one critical/high if bugs present)
    - False positive rate (dimension: false_positive_rate, min: 0, max: 30 percent)
    These are stubbed quality assertions (same as design-review Layer 2 — cannot programmatically invoke plugin from bash).

    **evals/fixtures/sample-pr.diff** (per D-45):
    Create an anonymized but realistic PR diff fixture. Use a TypeScript/JavaScript example with:
    - A clear bug (off-by-one error or null check missing) that any reviewer should catch
    - A style issue that should NOT be flagged (tests false positive filtering)
    - ~50-80 lines of diff content
    - Anonymized file paths (src/services/user.ts, src/utils/validate.ts, etc.)
    - Standard unified diff format with --- +++ @@ markers
  </action>
  <verify>
    <automated>cd /Users/felipemachado/Sites/spsk && bash evals/validate-structure.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - validate-structure.sh has Phase 3 section with 10+ new checks
    - All Phase 3 checks that CAN pass (file existence for plan-01 outputs) DO pass
    - assertions-code-review.json is valid JSON with 4+ assertions
    - sample-pr.diff is a realistic diff with at least one intentional bug
    - Structural validator exits 0 (all checks pass or SKIP)
  </acceptance_criteria>
  <done>Eval harness extended with code-review structural checks, quality assertions file created, and sample diff fixture bundled</done>
</task>

<task type="auto">
  <name>Task 2: Update run-evals.sh to include code-review layer</name>
  <files>evals/run-evals.sh</files>
  <read_first>
    - evals/run-evals.sh (existing orchestrator)
  </read_first>
  <action>
    Update the eval orchestrator to include code-review in its scope:
    - Add a section header for code-review quality evals (Layer 2b or similar)
    - Reference assertions-code-review.json
    - Keep it stubbed like design-review Layer 2 (note: programmatic plugin invocation not possible from bash)
    - Add a summary line that reports code-review assertion count alongside design-review
    - Ensure the structural validation (Layer 1) call still runs validate-structure.sh which now includes Phase 3 checks
  </action>
  <verify>
    <automated>grep -q "code-review" /Users/felipemachado/Sites/spsk/evals/run-evals.sh && grep -q "assertions-code-review" /Users/felipemachado/Sites/spsk/evals/run-evals.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - run-evals.sh references code-review assertions
    - run-evals.sh still runs structural validation
    - Script exits 0 when run
  </acceptance_criteria>
  <done>Eval orchestrator updated to cover both design-review and code-review skills</done>
</task>

</tasks>

<verification>
- `bash evals/validate-structure.sh` passes with 0 failures
- `bash evals/run-evals.sh` completes without error
- `jq empty evals/assertions-code-review.json` succeeds
- `wc -l evals/fixtures/sample-pr.diff` shows 20+ lines
</verification>

<success_criteria>
- Structural validator has Phase 3 checks for all code-review artifacts
- Quality assertions exist in assertions-code-review.json with range-based format
- Sample PR diff fixture is realistic and bundled
- run-evals.sh orchestrates both skills
</success_criteria>

<output>
After completion, create `.planning/phases/03-second-skill-release/03-02-SUMMARY.md`
</output>
