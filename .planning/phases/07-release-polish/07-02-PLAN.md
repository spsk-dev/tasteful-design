---
phase: 07-release-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - assets/demo.tape
  - VERSION
  - .claude-plugin/plugin.json
autonomous: false
requirements: [PLSH-01, PLSH-06]

must_haves:
  truths:
    - "VHS demo tape is updated for v1.1.0 and ready to record"
    - "VERSION file reads 1.1.0"
    - "Git tag v1.1.0 exists on the commit"
  artifacts:
    - path: "assets/demo.tape"
      provides: "Updated VHS tape with v1.1.0 branding and spsk-dev/tasteful-design footer"
      contains: "v1.1.0"
    - path: "VERSION"
      provides: "Version number"
      contains: "1.1.0"
  key_links:
    - from: "VERSION"
      to: ".claude-plugin/plugin.json"
      via: "version consistency"
      pattern: "1.1.0"
---

<objective>
Update VHS demo tape for v1.1.0, bump VERSION, update plugin.json version, and create the git tag.

Purpose: The demo tape needs v1.1.0 branding. VERSION and plugin.json must read 1.1.0 for the release. The git tag marks the release point.
Output: Updated demo.tape, VERSION at 1.1.0, plugin.json at 1.1.0, git tag v1.1.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-release-polish/07-01-SUMMARY.md

@assets/demo.tape
@VERSION
@.claude-plugin/plugin.json
@shared/output.md

<interfaces>
<!-- From shared/output.md -- branded output reference -->
Signature line format:
  SpSk  {command}  v{version}  ---  {specialist_count} specialists  .  tier {tier}

Footer:
  github.com/spsk-dev/tasteful-design

<!-- From plugin.json -- must update version -->
{
  "name": "tasteful-design",
  "version": "1.0.0",  <-- must become "1.1.0"
  ...
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update demo tape, VERSION, and plugin.json for v1.1.0</name>
  <files>assets/demo.tape, VERSION, .claude-plugin/plugin.json</files>
  <action>
**VERSION** (per D-117, PLSH-06):
Write `1.1.0` (no trailing newline) to VERSION file. Currently reads `1.0.0`.

**.claude-plugin/plugin.json**:
Update `"version": "1.0.0"` to `"version": "1.1.0"`. Also update the description to mention flow audit:
`"description": "AI-powered design review and flow audit -- 8 specialist agents score visual quality across 10 dimensions with multi-screen SPA analysis, cross-screen consistency detection, and HTML diagnostic reports"`

**assets/demo.tape** (per D-109, D-110, PLSH-01):
The tape already exists from Phase 2 and shows a /design-review run (per D-110 -- /design-review is more visually impressive in 30 seconds than /design-audit).

Updates needed (Plan 01 already fixed the footer in Task 2, so verify it says spsk-dev/tasteful-design):
1. Line 28 signature: confirm it reads `v1.1.0` (Plan 01 Task 2 should have updated this)
2. Line 119 footer: confirm it reads `github.com/spsk-dev/tasteful-design` (Plan 01 Task 2 should have fixed this)
3. If either is still stale, fix them now.

Per D-111: If VHS is not installed on this machine, that is OK -- do NOT block the release. The tape file is ready for recording whenever the user installs VHS. Add a comment at the top of demo.tape noting the record + optimize commands:
```
# To record: vhs assets/demo.tape
# To optimize: gifsicle --lossy=80 --optimize=3 --colors 64 -o assets/demo.gif assets/demo-raw.gif
```
(These comments already exist -- verify they are present.)

Check if VHS is available: `which vhs`. If available, run `vhs assets/demo.tape` to record, then optimize with gifsicle if available. If VHS is not available, skip recording -- the tape file is the deliverable.
  </action>
  <verify>
    <automated>cat VERSION | grep -q "1.1.0" && grep -q '"version": "1.1.0"' .claude-plugin/plugin.json && grep -q "v1.1.0" assets/demo.tape && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>VERSION reads 1.1.0. plugin.json version is 1.1.0 with updated description. Demo tape signature line says v1.1.0 and footer says spsk-dev/tasteful-design. If VHS was available, demo.gif was recorded and optimized.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify release artifacts and tag v1.1.0</name>
  <files>VERSION, .claude-plugin/plugin.json</files>
  <action>
Present all v1.1.0 release artifacts for user review before tagging.
After user approval, create git tag v1.1.0 (per D-118) and push to GitHub (per D-119):
  git tag -a v1.1.0 -m "v1.1.0: Flow audit, per-screen review, animation detection, HTML diagnostic reports"
  git push origin main --tags
  </action>
  <what-built>
All v1.1.0 release artifacts are ready:
- README.md with /design-audit documentation and flow examples
- ARCHITECTURE.md with flow audit component diagram
- CHANGELOG.md with v1.1.0 release notes
- All stale references fixed (spsk-dev/tasteful-design everywhere)
- VERSION at 1.1.0, plugin.json at 1.1.0
- Demo tape updated for v1.1.0

Remaining: git tag v1.1.0 and push (per D-118, D-119). This checkpoint confirms everything looks right before tagging.
  </what-built>
  <how-to-verify>
1. Open README.md -- verify the /design-audit section reads well, command table has the new entry, flow examples make sense
2. Open ARCHITECTURE.md -- verify the flow audit diagram is clear, file map includes new files
3. Open CHANGELOG.md -- verify v1.1.0 entry is above 1.0.0, lists all features
4. Run: `cat VERSION` -- should print `1.1.0`
5. Run: `grep -rn "felipemachado" --include="*.md" --include="*.sh" --include="*.json" --include="*.tape" . | grep -v ".planning/" | grep -v ".memsearch/" | grep -v ".context/" | grep -v ".claude/worktrees/"` -- should return nothing
6. If everything looks good, type "approved" and I will create the git tag v1.1.0 and push (per D-118, D-119)
  </how-to-verify>
  <verify>
    <automated>git tag -l v1.1.0 | grep -q "v1.1.0" && echo "PASS" || echo "FAIL: tag not yet created (awaiting approval)"</automated>
  </verify>
  <done>User has approved all release artifacts. Git tag v1.1.0 created and pushed to GitHub.</done>
  <resume-signal>Type "approved" to tag and push, or describe any issues to fix first</resume-signal>
</task>

</tasks>

<verification>
1. `cat VERSION` returns `1.1.0`
2. `grep '"version": "1.1.0"' .claude-plugin/plugin.json` matches
3. `grep "v1.1.0" assets/demo.tape` matches signature line
4. After approval: `git tag -l v1.1.0` returns `v1.1.0`
</verification>

<success_criteria>
- VERSION reads 1.1.0
- plugin.json version is 1.1.0
- Demo tape is v1.1.0-ready with correct footer
- User has approved the release artifacts
- Git tag v1.1.0 created and pushed to GitHub
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-polish/07-02-SUMMARY.md`
</output>
