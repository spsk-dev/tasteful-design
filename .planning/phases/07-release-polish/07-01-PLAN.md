---
phase: 07-release-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - ARCHITECTURE.md
  - CHANGELOG.md
  - assets/demo.tape
  - CLAUDE.md
  - evals/fixtures/report-test/flow-state.json
autonomous: true
requirements: [PLSH-02, PLSH-03, PLSH-04, PLSH-05]

must_haves:
  truths:
    - "README documents /design-audit usage with flow examples and --flow/--steps flags"
    - "ARCHITECTURE.md includes flow audit component diagram showing navigation -> review -> consistency -> report pipeline"
    - "CHANGELOG.md has v1.1.0 entry above 1.0.0 documenting flow audit, per-screen review, animation detection, and HTML report"
    - "All repo references use spsk-dev/tasteful-design consistently -- no felipemachado/spsk anywhere"
  artifacts:
    - path: "README.md"
      provides: "Flow audit documentation section"
      contains: "design-audit"
    - path: "ARCHITECTURE.md"
      provides: "Flow audit component diagram"
      contains: "Flow Navigation"
    - path: "CHANGELOG.md"
      provides: "v1.1.0 release notes"
      contains: "[1.1.0]"
  key_links:
    - from: "README.md"
      to: "ARCHITECTURE.md"
      via: "link to architecture docs"
      pattern: "ARCHITECTURE\\.md"
    - from: "README.md"
      to: "commands/design-audit.md"
      via: "documents the command usage"
      pattern: "design-audit"
---

<objective>
Update all documentation for v1.1.0 release and fix stale repo references.

Purpose: README must sell the flow audit feature, ARCHITECTURE.md must explain the new components, CHANGELOG must document what shipped, and all references must point to the correct org/repo.
Output: Updated README.md, ARCHITECTURE.md, CHANGELOG.md, plus stale reference fixes across the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@README.md
@ARCHITECTURE.md
@CHANGELOG.md
@shared/output.md
@commands/design-audit.md

<interfaces>
<!-- Current stale references to fix (per D-115) -->

From assets/demo.tape line 119:
  Type "github.com/felipemachado/spsk"
  -> Must become: Type "github.com/spsk-dev/tasteful-design"

From CLAUDE.md line 169:
  claude /install-plugin design-review@felipemachado/spsk
  -> Must become: claude /install-plugin tasteful-design@spsk-dev/tasteful-design

From evals/fixtures/report-test/flow-state.json lines 16, 48, 76:
  "screenshot_path": "/Users/felipemachado/Sites/spsk/evals/fixtures/report-test/..."
  -> Must use relative paths or a placeholder like "./evals/fixtures/report-test/..."

<!-- Files already correct (spsk-dev/tasteful-design): -->
- .claude-plugin/plugin.json (homepage)
- install.sh (REPO_URL)
- shared/output.md (footer)
- evals/validate-structure.sh (footer check)
- scripts/generate-report.sh (report footer)
- commands/design-init.md (footer)
- commands/design-audit.md (footer)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README, ARCHITECTURE, and CHANGELOG for v1.1.0</name>
  <files>README.md, ARCHITECTURE.md, CHANGELOG.md</files>
  <action>
**README.md** (per D-112, PLSH-02):

1. Add a `/design-audit` row to the Commands table:
   | `/design-audit` | Flow-level SPA design audit | `--flow "desc"`, `--steps url1,url2`, `--auth`, `--max N` |

2. Add a new "Flow Audit" section AFTER the "Code Review" section and BEFORE "Case Studies". Content:

   ## Flow Audit

   Multi-screen SPA design audit. Navigates through your app screen-by-screen, runs 8-specialist review on each screen, checks cross-screen consistency, and generates a self-contained HTML diagnostic report.

   ```bash
   # Audit a checkout flow (intent-guided navigation)
   /design-audit http://localhost:3000 --flow "complete the checkout"

   # Audit with explicit URL steps (deterministic)
   /design-audit http://localhost:3000 --steps /login,/dashboard,/settings

   # Audit a protected flow (authenticates first)
   /design-audit http://localhost:3000 --flow "onboarding" --auth
   ```

   **How it works:**
   1. Navigates SPA screens by clicking CTAs that match your flow description (or follows --steps URLs)
   2. Captures a screenshot at each screen state change (DOM stability detection)
   3. First and last screens get full 8-specialist review; middle screens get quick 4-specialist review
   4. Cross-screen consistency analysis flags visual drift (color, spacing, typography, button styles)
   5. Generates a self-contained HTML report with embedded screenshots, scores, and fix recommendations

   **Smart weighting:** The flow score uses position-weighted averaging -- first and last screens count 1.5x because they're the user's first impression and final experience.

   For the technical architecture, see [ARCHITECTURE.md](ARCHITECTURE.md#flow-audit).

3. Update the Architecture summary section to mention flow audit briefly: after the existing 5-phase pipeline description, add a paragraph:

   The flow audit pipeline (`/design-audit`) extends this to multi-screen flows -- navigating SPAs, running per-screen reviews with smart weighting, detecting cross-screen consistency drift, and generating HTML diagnostic reports.

4. Update the Requirements section to add: `**Playwright MCP** -- for flow audit navigation (npx @anthropic/mcp-server-playwright)`

**ARCHITECTURE.md** (per D-113, PLSH-03):

1. Add a new section "## Flow Audit Architecture" AFTER the existing "File Map" section (at the end). Content:

   ## Flow Audit Architecture

   The `/design-audit` command extends the single-page review to multi-screen SPA flows. It adds four components on top of the existing specialist system:

   ```
   User: /design-audit <url> --flow "checkout"
                    |
             +--------------+
             |  Navigation  |  Intent-guided or deterministic (--steps)
             |  Engine      |  Playwright MCP: snapshot -> reason -> click -> confirm
             +--------------+
                    |
            screen 1, screen 2, screen 3, ...
                    |
        +-----------+-----------+
        |                       |
   +----------+           +----------+
   | Full     |           | Quick    |  Smart weighting:
   | Review   |           | Review   |  first/last = 8 specialists
   | (8 spec) |           | (4 spec) |  middle = 4 specialists
   +----------+           +----------+
        |                       |
        +-----------+-----------+
                    |
             +--------------+
             |  Consistency |  Cross-screen drift detection:
             |  Analysis    |  colors, spacing, typography,
             +--------------+  buttons, components
                    |
             +--------------+
             |  Report      |  Self-contained HTML with
             |  Generator   |  embedded JPEG screenshots,
             +--------------+  scores, fix recommendations
                    |
              flow-report.html
   ```

   ### Navigation Engine

   Two navigation modes:
   - **Intent mode** (`--flow "description"`): Agent reads Playwright MCP snapshots, identifies CTAs matching the flow intent, clicks them, and confirms screen transitions via DOM stability detection (800ms mutation quiet period).
   - **Deterministic mode** (`--steps url1,url2`): Visits URLs in order with no CTA clicking. Continues to next URL on navigation failure.

   Screen detection uses DOM mutation observation (not networkidle) with a configurable quiet period. Screenshots are captured at 1440x900 viewport after font readiness confirmation.

   ### Smart Weighting

   Not all screens in a flow deserve equal review depth:
   - **First screen** (entry point): Full 8-specialist review (1.5x score weight)
   - **Last screen** (completion): Full 8-specialist review (1.5x score weight)
   - **Middle screens**: Quick 4-specialist review (1.0x score weight)

   This balances token budget against review thoroughness -- the user's first impression and final experience get the most scrutiny.

   ### Cross-Screen Consistency

   After all screens are reviewed, a consistency pass compares five dimensions across the flow:
   - **Color palette** (Delta E >= 10 flags drift)
   - **Spacing** (4px tolerance)
   - **Typography** (2px font size tolerance)
   - **Button styles** (shape, color, sizing)
   - **Component variants** (card styles, input patterns)

   Findings are severity-graded (drift/mismatch/conflict) and apply a penalty of up to 15% to the flow score.

   ### HTML Diagnostic Report

   The report generator (`scripts/generate-report.sh`) produces a self-contained HTML file:
   - Flow map showing screen progression (1 -> 2 -> 3 -> ...)
   - Base64-embedded JPEG screenshots (auto-recompressed to stay under 4MB)
   - Per-screen specialist scores with expandable detail sections
   - Overall flow score with top 5 priority fixes
   - Print-to-PDF support via `@media print` styles

2. Update the File Map at the end to include the new files:
   Add under the existing tree:
   ```
   +-- commands/
   |   +-- design-audit.md     # /design-audit -- flow-level SPA audit
   +-- config/
   |   +-- flow-scoring.json   # Flow navigation and scoring config
   +-- scripts/
   |   +-- generate-report.sh  # HTML diagnostic report generator
   +-- skills/
   |   +-- design-review/
   |       +-- references/
   |           +-- flow.md     # Flow navigation reference
   |           +-- animation.md # Animation detection reference
   |           +-- consistency.md # Cross-screen consistency reference
   +-- evals/
   |   +-- fixtures/
   |       +-- flow-test/      # 3-page static HTML fixture for flow smoke tests
   |       +-- report-test/    # Mock flow-state.json for report generation tests
   ```

**CHANGELOG.md** (per D-114, PLSH-04):

Add a new `[1.1.0]` section ABOVE the existing `[1.0.0]` entry. Content:

```
## [1.1.0] - 2026-03-30

### Added

- Flow-level SPA design audit (`/design-audit` command) with intent-guided and deterministic navigation modes
- Playwright MCP integration for stateful browser navigation with DOM stability detection
- Per-screen 8-specialist review with smart weighting (full review on first/last, quick on middle screens)
- Cross-screen consistency analysis detecting visual drift in colors, spacing, typography, buttons, and components
- Animation detection with CSS transition/property analysis and `prefers-reduced-motion` compliance checking
- Self-contained HTML diagnostic report with base64-embedded JPEG screenshots, flow map, expandable specialist details, and print-to-PDF support
- Flow score aggregation with position-weighted averaging (1.5x for first and last screens)
- Consistency penalty system (up to 15% score reduction for cross-screen drift)
- Authentication gate for auditing protected flows (`--auth` flag)
- Static HTML test fixtures for flow navigation smoke testing
- Report generation test fixtures with mock flow state

### Changed

- Plugin manifest updated with `design-audit` command
- Score bars in HTML report use CSS percentage-width divs (not Unicode blocks)
- Report generation is non-blocking -- terminal summary is the primary output, HTML report is a bonus
```

  </action>
  <verify>
    <automated>grep -q "design-audit" README.md && grep -q "Flow Audit Architecture" ARCHITECTURE.md && grep -q "\[1.1.0\]" CHANGELOG.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README has /design-audit section with flow examples and command table entry. ARCHITECTURE.md has flow audit component diagram with navigation engine, smart weighting, consistency, and report sections plus updated file map. CHANGELOG.md has v1.1.0 entry with all Added/Changed items above the 1.0.0 entry.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all stale repo references</name>
  <files>assets/demo.tape, CLAUDE.md, evals/fixtures/report-test/flow-state.json</files>
  <action>
Per D-115 and D-116 (PLSH-05), fix these stale references:

1. **assets/demo.tape line 119**: Change `Type "github.com/felipemachado/spsk"` to `Type "github.com/spsk-dev/tasteful-design"`. Also update the signature line on line 28 from `v1.0.0` to `v1.1.0` since this tape will be used for the v1.1.0 demo.

2. **CLAUDE.md**: Find the line containing `design-review@felipemachado/spsk` and replace with `tasteful-design@spsk-dev/tasteful-design` (per D-116). The correct install command is: `claude /install-plugin tasteful-design@spsk-dev/tasteful-design`

3. **evals/fixtures/report-test/flow-state.json**: Replace all 3 absolute paths:
   - `/Users/felipemachado/Sites/spsk/evals/fixtures/report-test/screen-1-login.png` -> `./evals/fixtures/report-test/screen-1-login.png`
   - `/Users/felipemachado/Sites/spsk/evals/fixtures/report-test/screen-2-dashboard.png` -> `./evals/fixtures/report-test/screen-2-dashboard.png`
   - `/Users/felipemachado/Sites/spsk/evals/fixtures/report-test/screen-3-settings.png` -> `./evals/fixtures/report-test/screen-3-settings.png`
   These should be relative paths so the fixture works on any machine.

4. **Verification sweep**: After fixes, run `grep -rn "felipemachado" --include="*.md" --include="*.sh" --include="*.json" --include="*.tape" . | grep -v ".planning/" | grep -v ".memsearch/" | grep -v ".context/" | grep -v ".claude/worktrees/"` and confirm zero results. If any remain, fix them.

Already correct (do NOT change):
- .claude-plugin/plugin.json (homepage: spsk-dev/tasteful-design)
- install.sh (REPO_URL: spsk-dev/tasteful-design)
- shared/output.md (footer: spsk-dev/tasteful-design)
- evals/validate-structure.sh (checks for spsk-dev/tasteful-design)
- scripts/generate-report.sh (report footer: spsk-dev/tasteful-design)
- commands/design-init.md, commands/design-audit.md (footers: spsk-dev/tasteful-design)
  </action>
  <verify>
    <automated>grep -rn "felipemachado" --include="*.md" --include="*.sh" --include="*.json" --include="*.tape" . | grep -v ".planning/" | grep -v ".memsearch/" | grep -v ".context/" | grep -v ".claude/worktrees/" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS: No stale references" || echo "FAIL: Stale references remain"</automated>
  </verify>
  <done>Zero instances of "felipemachado" in any .md, .sh, .json, or .tape file outside of .planning/, .memsearch/, .context/, and .claude/worktrees/ directories. All install commands reference tasteful-design@spsk-dev/tasteful-design. All fixture paths are relative.</done>
</task>

</tasks>

<verification>
1. `grep -q "design-audit" README.md` -- README documents the command
2. `grep -q "Flow Audit Architecture" ARCHITECTURE.md` -- Architecture has the new section
3. `grep -q "\[1.1.0\]" CHANGELOG.md` -- Changelog has the release entry
4. `grep -rn "felipemachado" --include="*.md" --include="*.sh" --include="*.json" --include="*.tape" . | grep -v ".planning/" | grep -v ".memsearch/" | grep -v ".context/" | grep -v ".claude/worktrees/" | wc -l` returns 0
5. `grep -q "tasteful-design@spsk-dev/tasteful-design" CLAUDE.md` -- Install command correct
</verification>

<success_criteria>
- README has /design-audit section with usage examples, updated command table, and architecture reference
- ARCHITECTURE.md has flow audit diagram with all four components (navigation, review, consistency, report) and updated file map
- CHANGELOG.md has v1.1.0 release notes with all v1.1.0 features documented
- Zero stale felipemachado references in repo source files
- All install commands point to tasteful-design@spsk-dev/tasteful-design
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-polish/07-01-SUMMARY.md`
</output>
