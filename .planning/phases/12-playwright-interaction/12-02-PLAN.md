---
phase: 12-playwright-interaction
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - CLAUDE.md
  - commands/design-review.md
autonomous: false
requirements: [TEST-01]

must_haves:
  truths:
    - "Running /design-audit on start.fusefinance.com completes a flow audit with real SPA navigation"
    - "The design-audit command navigates through at least 2 screens on the live site"
    - "Flow-state.json is produced with screen entries containing URLs, screenshots, and navigation data"
    - "CLAUDE.md documents the --interact flag alongside existing command documentation"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated command documentation with --interact flag"
      contains: "--interact"
    - path: "commands/design-review.md"
      provides: "Any minor fixes discovered during e2e validation"
  key_links:
    - from: "commands/design-audit.md"
      to: "start.fusefinance.com"
      via: "browser_navigate through live SPA"
      pattern: "start.fusefinance.com"
---

<objective>
Validate the complete design-audit pipeline end-to-end on a real production SPA (start.fusefinance.com), update CLAUDE.md documentation with the new --interact flag, and fix any issues discovered during the live validation.

Purpose: Prove the Playwright MCP flow audit pipeline works against a real production site, not just test fixtures. Update user-facing docs.
Output: Validated pipeline, updated CLAUDE.md, any bugfixes.
</objective>

<execution_context>
@/Users/felipemachado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/felipemachado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-playwright-interaction/12-CONTEXT.md
@.planning/phases/12-playwright-interaction/12-01-SUMMARY.md

@commands/design-audit.md
@commands/design-review.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md documentation with --interact flag</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file -- find the /design-review command docs and flags table)
    - commands/design-review.md (first 60 lines -- see the --interact parsing added in Plan 01)
  </read_first>
  <action>
Update the CLAUDE.md documentation for the design-review plugin:

1. **In the `/design-review` usage examples** (the bash code block showing command examples), add:
```bash
/design-review --interact                  # Capture hover/focus/scroll states before review
/design-review --interact --quick           # Quick review with interaction capture
```

2. **In the Flags table** (the markdown table after the examples), add a row:
```
| `--interact` | Capture hover/focus/scroll states via Playwright MCP before specialist review. Opt-in. |
```

3. **In the `/design` orchestrator section**, if the flags list mentions available passthrough flags, add `--interact` to that list.

4. **In the "Available Commands" section**, update the `/design-review` one-liner description from the current text to: "Full 7-specialist review with weighted scoring and SHIP/CONDITIONAL/BLOCK verdict. Supports `--interact` for hover/focus/scroll state capture."

Do NOT rewrite existing documentation -- only add the --interact references. Keep all existing content intact.
  </action>
  <verify>
    <automated>grep -c "\-\-interact" CLAUDE.md | xargs test 3 -le && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\-\-interact" CLAUDE.md returns at least 3 matches
    - grep "hover/focus/scroll" CLAUDE.md returns at least 1 match
    - Existing CLAUDE.md content (project description, constraints, technology stack) is unchanged
  </acceptance_criteria>
  <done>CLAUDE.md documents --interact flag in usage examples, flags table, and command descriptions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation on start.fusefinance.com</name>
  <files>commands/design-review.md</files>
  <action>
Run `/design-audit start.fusefinance.com --flow "explore the landing page" --max-screens 3` in a Claude Code session with the design-review plugin installed and Playwright MCP registered. This validates the complete pipeline (flow navigation, per-screen review, HTML report) against a real production SPA. If start.fusefinance.com is unavailable or behind auth, use an alternative public SPA (linear.app, vercel.com). Fix any issues discovered during the validation in commands/design-review.md or commands/design-audit.md.
  </action>
  <verify>
    <automated>echo "Manual checkpoint -- human verifies audit completed on live SPA"</automated>
  </verify>
  <acceptance_criteria>
    - design-audit navigated a real SPA and produced flow-state.json with at least 2 screen entries
    - No MCP failures, timeout crashes, or unrecoverable errors during the audit
    - Screenshots were captured at each screen state change
  </acceptance_criteria>
  <done>The design-audit pipeline has been validated end-to-end on a real production SPA with successful multi-screen navigation, screenshot capture, and flow-state.json generation</done>
</task>

</tasks>

<verification>
1. `grep -c "\-\-interact" CLAUDE.md` returns >= 3
2. The design-audit pipeline navigated a real SPA without crashing
3. Flow-state.json was produced with at least 2 screen entries
4. All structural validation checks still pass: `bash evals/validate-structure.sh`
</verification>

<success_criteria>
- CLAUDE.md is updated with --interact documentation matching the implementation from Plan 01
- The design-audit pipeline has been validated against a real production SPA with successful navigation through multiple screens
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/12-playwright-interaction/12-02-SUMMARY.md`
</output>
