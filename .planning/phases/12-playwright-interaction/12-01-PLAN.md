---
phase: 12-playwright-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/design-review.md
  - commands/design.md
  - evals/validate-structure.sh
autonomous: true
requirements: [INTR-01, INTR-02, INTR-03]

must_haves:
  truths:
    - "Running /design-review <url> --interact triggers Playwright MCP interactions (hover, focus, scroll) before specialist scoring"
    - "The baseline-interact-reset pattern is followed: clean screenshot first, then interactions, then page reload, then standard review on clean DOM"
    - "No more than 8 interactions are performed per review (budget cap enforced)"
    - "Running /design-review <url> without --interact behaves identically to before (opt-in only)"
    - "Interaction screenshots are passed to Motion, Code/A11y, and Color/Layout specialists as additional context"
  artifacts:
    - path: "commands/design-review.md"
      provides: "--interact flag parsing in Mode section, Phase 0.5i interaction protocol between Phase 0 and Phase 1"
      contains: "--interact"
    - path: "commands/design.md"
      provides: "Updated router with --interact flag passthrough"
      contains: "--interact"
    - path: "evals/validate-structure.sh"
      provides: "Structural checks for interaction protocol presence"
      contains: "interact"
  key_links:
    - from: "commands/design-review.md (Phase 0.5i)"
      to: "Playwright MCP tools"
      via: "browser_navigate, browser_snapshot, browser_hover, browser_click, browser_evaluate, browser_take_screenshot"
      pattern: "browser_snapshot.*browser_hover"
    - from: "commands/design-review.md (Phase 0.5i)"
      to: "commands/design-review.md (Phase 2)"
      via: "INTERACTION_SCREENSHOTS variable passed to specialist dispatch"
      pattern: "INTERACTION_SCREENSHOTS"
    - from: "commands/design-review.md (Phase 0.5i)"
      to: "page reload"
      via: "browser_navigate to reload after interactions, before standard review"
      pattern: "browser_navigate.*reload"
---

<objective>
Add the `--interact` flag to `/design-review` that triggers Playwright MCP-driven hover, focus, and scroll interactions before specialist scoring. The flag follows a baseline-interact-reset pattern: capture clean screenshots first, perform up to 8 interactions capturing screenshots of each, reload the page, then run the standard specialist review on clean DOM. Interaction screenshots are passed as additional context to Motion, Code/A11y, and Color/Layout specialists.

Purpose: Give specialists richer state information (hover states, focus rings, scroll reveals) without mutating the page they score against.
Output: Modified design-review.md with interaction protocol, updated router, structural validation checks.
</objective>

<execution_context>
@/Users/felipemachado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/felipemachado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-playwright-interaction/12-CONTEXT.md

@commands/design-review.md
@commands/design-audit.md
@commands/design.md
@skills/design-review/references/flow.md
</context>

<interfaces>
<!-- Key patterns from design-audit.md that the executor should reuse for interaction protocol -->

From commands/design-audit.md (Playwright MCP interaction patterns):
- browser_navigate: Call with URL to load or reload page
- browser_snapshot: Returns accessibility tree -- use to discover interactive elements (buttons, links, inputs, hover targets)
- browser_hover: Call with ref="{element_ref}" from snapshot to trigger hover state
- browser_click: Call with ref="{element_ref}" to click/focus elements
- browser_evaluate: Inject JS for scroll actions, DOM state queries
- browser_take_screenshot: Capture viewport as PNG
- browser_resize: Set viewport dimensions (1440x900)

From commands/design-review.md (current Phase 0 flow):
- Phase 0: Screenshots via npx playwright screenshot CLI (desktop, mobile, fold)
- Phase 0.5: Load .design/ project context
- Phase 1: Haiku agent page classification
- Phase 2: Specialist dispatch -- specialists receive screenshots + source files + PAGE_BRIEF

From commands/design-review.md (existing argument parsing):
- Mode section parses $ARGUMENTS for --quick
- Tier detection: GEMINI_AVAILABLE, PLAYWRIGHT_AVAILABLE
- Review dir: REVIEW_DIR="/tmp/design-review-$(date +%Y%m%d-%H%M%S)"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add --interact flag parsing and Phase 0.5i interaction protocol to design-review.md</name>
  <files>commands/design-review.md</files>
  <read_first>
    - commands/design-review.md (full file -- understand Phase 0, 0.5, 1, 2 structure)
    - commands/design-audit.md (lines 106-170 -- Playwright MCP verification and snapshot patterns)
    - skills/design-review/references/flow.md (lines 1-50 -- DOM stability pattern)
    - .planning/phases/12-playwright-interaction/12-CONTEXT.md (implementation decisions)
  </read_first>
  <action>
Three changes to design-review.md:

**Change 1: Add --interact to Mode section (after the --quick parsing, around line 46-54)**

Add a new subsection under "### Mode (parse `$ARGUMENTS`)":

```
### Interaction capture (parse `$ARGUMENTS`)

- **`--interact`**: Opt-in Playwright MCP interaction capture. Triggers hover, focus, and scroll interactions before specialist scoring. Requires Playwright MCP (Tier 1 or 2). If Tier 3 (no Playwright), warn: "Cannot use --interact without Playwright. Install with: npx playwright install chromium" and ignore the flag.
- **Default (no `--interact`)**: Standard screenshot-only review. No behavior change.

Store: `INTERACT_MODE=true` if `--interact` present, `false` otherwise.
```

**Change 2: Insert "Phase 0.5i: Interaction Capture" as a NEW section between Phase 0 (Screenshots) and Phase 0.5 (Load Project Design Context)**

This entire section only runs when `INTERACT_MODE=true`. Write it as a numbered phase section matching the existing document style (## heading, steps with code blocks). The protocol is:

```markdown
## Phase 0.5i: Interaction Capture (only when --interact)

This phase runs ONLY when `--interact` flag is present. It follows the baseline-interact-reset pattern:
1. Phase 0 already captured BASELINE screenshots (clean state)
2. This phase captures INTERACTION state (hover, focus, scroll)
3. After interactions, the page is RELOADED so Phase 1+ sees clean DOM

### 0.5i-a. Launch Playwright MCP session

Call `browser_navigate` with `$DEV_URL` to open the page in a persistent browser session.

**If browser_navigate fails** (MCP not registered), show:
```
⚠ --interact requires Playwright MCP. Run:
  claude mcp add playwright -- npx @playwright/mcp@latest

Falling back to standard screenshot-only review.
```
Set `INTERACT_MODE=false` and skip to Phase 0.5.

Call `browser_resize` to set viewport 1440x900.

### 0.5i-b. Discover interactive elements

Call `browser_snapshot` to get the accessibility tree.

Identify up to 8 interaction targets from the snapshot, prioritized:
1. **Hover targets**: Buttons, links, cards, nav items -- elements likely to have hover states (background change, shadow, underline, scale)
2. **Focus targets**: Form inputs, interactive controls -- elements that show focus rings or focus styles
3. **Scroll reveals**: If the page has sections below the fold, scroll to reveal them (likely lazy-loaded content, scroll-triggered animations)

**Budget cap: 8 interactions maximum.** Select the 8 most design-relevant elements. Prioritize:
- Primary CTA buttons (1-2 hover interactions)
- Navigation links (1 hover interaction)
- Form inputs if present (1-2 focus interactions)
- Cards or interactive panels (1-2 hover interactions)
- Below-fold scroll (1 scroll interaction)

Store the list as `INTERACTION_TARGETS` with element refs from the snapshot.

### 0.5i-c. Execute interactions and capture

Create an interaction screenshots directory:
```
INTERACT_DIR="$REVIEW_DIR/interactions"
mkdir -p "$INTERACT_DIR"
```

For each target (up to 8):

**Hover interaction:**
1. Call `browser_hover` with `ref="{element_ref}"`
2. Wait 300ms (let CSS transitions complete)
3. Call `browser_take_screenshot` -- save to `$INTERACT_DIR/interact-{N}-hover-{element_desc}.png`

**Focus interaction:**
1. Call `browser_click` with `ref="{element_ref}"` (clicking focuses the element)
2. Wait 200ms
3. Call `browser_take_screenshot` -- save to `$INTERACT_DIR/interact-{N}-focus-{element_desc}.png`

**Scroll interaction:**
1. Call `browser_evaluate` with: `window.scrollBy(0, window.innerHeight)`
2. Wait 500ms (let scroll-triggered animations and lazy loads complete)
3. Call `browser_take_screenshot` -- save to `$INTERACT_DIR/interact-{N}-scroll-{section_desc}.png`

Track each interaction in `INTERACTION_LOG`:
```json
[
  {"type": "hover", "element": "button.cta-primary", "ref": "e45", "screenshot": "interact-1-hover-cta-primary.png"},
  {"type": "focus", "element": "input.email", "ref": "e72", "screenshot": "interact-2-focus-email-input.png"},
  {"type": "scroll", "element": "viewport", "ref": null, "screenshot": "interact-3-scroll-features-section.png"}
]
```

Count interactions. If count reaches 8, stop and note:
```
✓ Interaction budget reached (8/8). Proceeding to reset.
```

### 0.5i-d. Reset page state

After all interactions, reload the page to restore clean DOM:

1. Call `browser_navigate` with `$DEV_URL` (re-navigates to the same URL)
2. Run DOM stability check via `browser_evaluate` (same MutationObserver pattern from design-audit.md Step D):
```javascript
(function() {
  return new Promise((resolve) => {
    let timer = null;
    const observer = new MutationObserver(() => {
      clearTimeout(timer);
      timer = setTimeout(() => { observer.disconnect(); resolve({ stable: true }); }, 800);
    });
    observer.observe(document.body, { childList: true, subtree: true, attributes: true, characterData: true });
    timer = setTimeout(() => { observer.disconnect(); resolve({ stable: true, waited: false }); }, 2000);
  });
})()
```
3. Call `browser_close` to release the MCP session

The page is now clean. Specialists in Phase 2 will evaluate the baseline screenshots (from Phase 0), not the interaction-mutated state.

### 0.5i-e. Store interaction context

Store `INTERACTION_LOG` and `INTERACT_DIR` path for Phase 2 specialist dispatch.

Show summary:
```
✓ Interaction capture complete: {N} interactions ({hover_count} hover, {focus_count} focus, {scroll_count} scroll)
```
```

**Change 3: Update Phase 2 specialist dispatch to pass interaction screenshots**

In Phase 2, after the existing "Context prefix for EVERY specialist prompt" block, add a conditional block for when INTERACT_MODE is true:

```markdown
**If `INTERACT_MODE` is true**, append interaction context to the following specialists ONLY:

- **Specialist 2: Color** -- pass interaction hover screenshots showing color state changes
- **Specialist 3: Layout** -- pass interaction hover/scroll screenshots showing layout shifts
- **Specialist 5: Motion** -- pass ALL interaction screenshots (hover transitions, focus animations, scroll reveals)
- **Specialist 7: Code & Accessibility** -- pass focus interaction screenshots (focus ring visibility, focus order)

Append to these specialists' prompts:
```
INTERACTION CONTEXT: The reviewer captured {N} interaction states before this review.
These screenshots show hover states, focus rings, and scroll-revealed content.
Evaluate these interaction states alongside the baseline screenshots.
- Are hover states well-designed? (Color change, shadow, scale — not just cursor change)
- Are focus rings visible and accessible? (Minimum 2:1 contrast ratio)
- Do scroll-triggered elements load/animate gracefully?
- Flag any interaction state that feels broken, missing, or inconsistent with the baseline.
```

Pass the interaction screenshot files from `$INTERACT_DIR/` alongside the regular screenshots.

Specialists 1 (Font), 4 (Icon), and 6 (Intent) do NOT receive interaction screenshots -- their domains are not affected by hover/focus/scroll states.
```

Do NOT change the standard review flow -- when --interact is absent, everything works identically to before.
  </action>
  <verify>
    <automated>grep -c "\-\-interact" commands/design-review.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep "Phase 0.5i" commands/design-review.md returns at least 1 match
    - grep "\-\-interact" commands/design-review.md returns at least 5 matches
    - grep "INTERACT_MODE" commands/design-review.md returns at least 3 matches
    - grep "browser_hover" commands/design-review.md returns at least 1 match
    - grep "browser_navigate.*reload\|reload.*browser_navigate\|re-navigates" commands/design-review.md returns at least 1 match
    - grep "budget.*8\|8.*interaction" commands/design-review.md returns at least 1 match
    - grep "INTERACTION_LOG\|INTERACT_DIR\|INTERACTION_SCREENSHOTS" commands/design-review.md returns at least 2 matches
    - The existing Phase 0, Phase 0.5, Phase 1, Phase 2-5 sections are unchanged in structure
  </acceptance_criteria>
  <done>design-review.md contains a complete Phase 0.5i interaction protocol with --interact flag parsing, 8-interaction budget cap, baseline-interact-reset pattern (navigate, snapshot, hover/focus/scroll, reload, close), and specialist dispatch integration for Motion, Code/A11y, Color, and Layout</done>
</task>

<task type="auto">
  <name>Task 2: Update router passthrough and add structural validation checks</name>
  <files>commands/design.md, evals/validate-structure.sh</files>
  <read_first>
    - commands/design.md (full file -- see existing flag passthrough list)
    - evals/validate-structure.sh (last 50 lines -- see check patterns and numbering)
  </read_first>
  <action>
Two files to update:

**File 1: commands/design.md**

1. In the "All flags pass through" line (around line 35), add `--interact` to the list. Current list ends with `--max-screens`. Add `--interact` after it.

2. In the `/design` (no arguments) menu (around line 110-124), add `--interact` to the Flags section:
```
  --interact               Capture hover/focus/scroll states before review
```
Add it after the `--max-screens` entry.

3. In the routing table (around line 22-33), no new route needed -- `--interact` passes through to `/design-review` via the existing `/design review` route. But update the `/design review` description from "Visual review (7 specialists)" to "Visual review (7 specialists, supports --interact)".

**File 2: evals/validate-structure.sh**

Add 4 new structural checks at the end of the existing check sequence (find the last check number and continue from there). Follow the exact pattern used by existing checks (the script uses a CHECK_NUM counter and check_exists/check_contains/check_not_contains functions -- read the file to confirm the pattern).

New checks:
1. Check that `commands/design-review.md` contains `--interact` (flag is documented)
2. Check that `commands/design-review.md` contains `Phase 0.5i` (interaction protocol exists)
3. Check that `commands/design-review.md` contains `browser_hover` (Playwright MCP hover is used)
4. Check that `commands/design-review.md` contains `INTERACT_MODE` (flag state variable exists)

Follow the exact naming and formatting conventions of the existing checks. The check descriptions should be prefixed with "Phase 12:" for traceability.
  </action>
  <verify>
    <automated>grep -c "\-\-interact" commands/design.md | xargs test 2 -le && grep -c "Phase 12" evals/validate-structure.sh | xargs test 3 -le && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\-\-interact" commands/design.md returns at least 2 matches (passthrough line + menu)
    - grep "Phase 12" evals/validate-structure.sh returns at least 4 matches (one per new check)
    - Running bash evals/validate-structure.sh exits 0 (all checks pass including new ones)
    - The existing routing table and flag list are preserved, --interact is additive only
  </acceptance_criteria>
  <done>design.md routes --interact through to /design-review, shows it in the help menu. validate-structure.sh has 4 new Phase 12 checks confirming the interaction protocol is properly wired in design-review.md</done>
</task>

</tasks>

<verification>
1. `grep -c "\-\-interact" commands/design-review.md` returns >= 5
2. `grep "Phase 0.5i" commands/design-review.md` returns match
3. `grep "browser_hover" commands/design-review.md` returns match
4. `grep "\-\-interact" commands/design.md` returns >= 2
5. `bash evals/validate-structure.sh` exits 0 (all structural checks pass)
6. The standard review flow (without --interact) is unmodified -- no regression
</verification>

<success_criteria>
- /design-review --interact triggers a 5-step interaction protocol (navigate, snapshot, interact up to 8 elements, reload, close) between Phase 0 screenshots and Phase 1 page analysis
- The baseline-interact-reset pattern ensures specialists never see interaction-mutated DOM as baseline
- Interaction screenshots go to Motion, Code/A11y, Color, and Layout specialists only
- The --interact flag is opt-in with graceful degradation (Tier 3 warns and skips)
- All structural validation checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-playwright-interaction/12-01-SUMMARY.md`
</output>
