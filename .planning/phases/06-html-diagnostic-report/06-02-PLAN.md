---
phase: 06-html-diagnostic-report
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - commands/design-audit.md
autonomous: false
requirements: [REPT-01, REPT-04]

must_haves:
  truths:
    - "After /design-audit completes, the HTML report is automatically generated without user intervention"
    - "The report file path is printed to the terminal with SpSk branding"
    - "The report contains the same data shown in the terminal summary"
  artifacts:
    - path: "commands/design-audit.md"
      provides: "Section 15 calling generate-report.sh"
      contains: "generate-report.sh"
  key_links:
    - from: "commands/design-audit.md"
      to: "scripts/generate-report.sh"
      via: "Section 15 bash call with flow-state.json path"
      pattern: "generate-report\\.sh"
    - from: "commands/design-audit.md"
      to: "flow-state.json"
      via: "AUDIT_DIR path variable passed to script"
      pattern: "AUDIT_DIR.*flow-state\\.json"
---

<objective>
Wire generate-report.sh into the design-audit command as Section 15, and verify the full integration with a human checkpoint.

Purpose: The report must generate automatically at the end of every flow audit -- not as a separate manual step.
Output: Updated design-audit.md with Section 15, human-verified report in browser
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-html-diagnostic-report/06-CONTEXT.md
@.planning/phases/06-html-diagnostic-report/06-01-SUMMARY.md
@commands/design-audit.md
@shared/output.md

<interfaces>
<!-- From design-audit.md current structure (14 sections) -->
Section 14 is the final section: Flow Review Summary (14a-14g)
Section 14f shows: "Next: Run Phase 6 to generate the HTML diagnostic report"
Section 14g is the footer

The command stores AUDIT_DIR as the output directory path.
flow-state.json lives at {AUDIT_DIR}/flow-state.json.
Screenshots are at {AUDIT_DIR}/screen-{N}-{slug}.png.

<!-- From Plan 01 output -->
scripts/generate-report.sh accepts:
  $1: path to flow-state.json (required)
  $2: output HTML path (optional, defaults to report-{timestamp}.html in same dir)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 15 to design-audit.md for report generation</name>
  <files>commands/design-audit.md</files>
  <action>
Add a new Section 15 to design-audit.md after Section 14 (Flow Review Summary). Per D-107 and D-108.

**Section 15: Generate HTML Diagnostic Report**

Place this AFTER the Section 14 footer (14g) and BEFORE the section count comment at the bottom.

Content:

```markdown
## 15. Generate HTML Diagnostic Report

After the flow review summary (Section 14), generate the self-contained HTML report.

### 15a. Call report generator

Run the report generator script:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/generate-report.sh" "${AUDIT_DIR}/flow-state.json" "${AUDIT_DIR}/report-$(date -u +%Y%m%dT%H%M%SZ).html"
```

Store the output path as `REPORT_PATH`.

**If the script fails** (non-zero exit):
- Show warning: `Warning: HTML report generation failed. Flow results are still available in flow-state.json.`
- Do NOT fail the entire audit -- the terminal summary from Section 14 already shows results
- Log the error to flow-state.json: `"report_error": "{error message}"`

### 15b. Update flow-state.json

Add the report path to flow-state.json:
```json
{
  "report_path": "{REPORT_PATH}"
}
```

### 15c. Display report path (per D-108)

Show branded output:
```
+-- REPORT --------------------------------------------------+
|  check HTML report generated                                |
|  Path: {REPORT_PATH}                                        |
|                                                              |
|  Open in browser to view full diagnostic with screenshots   |
+-------------------------------------------------------------+
```
```

Also update:
1. **Section 14f** -- change "Next: Run Phase 6 to generate the HTML diagnostic report" to "Next: Generating HTML diagnostic report..." (since it now happens automatically)
2. **Key Constraints** -- add: "Section 15 runs after Section 14 even if some reviews had errors. The report shows whatever data is available."
3. **Section count comment** at bottom -- update from 14 to 15 sections
4. **allowed-tools** in frontmatter -- no changes needed (already has `Bash(*)` patterns)
  </action>
  <verify>
    <automated>bash -c "cd /Users/felipemachado/Sites/spsk && grep -c 'Section 15' commands/design-audit.md && grep -q 'generate-report.sh' commands/design-audit.md && grep -q '15 sections' commands/design-audit.md && echo 'INTEGRATION OK'"</automated>
  </verify>
  <done>
    - design-audit.md has Section 15 calling generate-report.sh
    - Section 14f updated to indicate automatic report generation
    - Key Constraints updated with report generation rule
    - Section count comment updated to 15
    - Report failure is non-blocking (warning only)
    - Report path stored in flow-state.json
    - Branded output box shows the report path
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of HTML report in browser</name>
  <files>commands/design-audit.md</files>
  <action>
Human verifies the complete HTML diagnostic report renders correctly in a browser and prints cleanly to PDF. The report was built by Plan 01 (generate-report.sh) and wired into design-audit.md by Task 1.

Steps for the human:
1. Run: `scripts/generate-report.sh evals/fixtures/report-test/flow-state.json /tmp/spsk-report-check.html`
2. Open `/tmp/spsk-report-check.html` in browser
3. Verify: dark theme, flow map with 3 thumbnails, clickable scroll-to-section, screenshot rendering, CSS score bars, collapsible details, expand-all button, flow score verdict, top 5 fixes, consistency findings, SpSk branding
4. Test print: Cmd+P -- background white, all details expanded, clean page flow
5. Check file size under 5MB
  </action>
  <verify>
    <automated>bash -c "scripts/generate-report.sh evals/fixtures/report-test/flow-state.json /tmp/spsk-report-check.html && test -f /tmp/spsk-report-check.html && echo 'FILE EXISTS'"</automated>
  </verify>
  <done>
    - Human confirms dark theme renders correctly
    - Human confirms flow map thumbnails are clickable and scroll to sections
    - Human confirms specialist details collapse/expand
    - Human confirms print-to-PDF produces clean output
    - Human types "approved"
  </done>
</task>

</tasks>

<verification>
1. `grep -q 'generate-report.sh' commands/design-audit.md` -- Section 15 exists
2. `scripts/generate-report.sh evals/fixtures/report-test/flow-state.json /tmp/report.html` succeeds
3. Report opens in browser with all sections rendering
4. Print preview shows clean, expanded layout
5. `evals/validate-structure.sh` passes all checks including new report checks from Plan 01
</verification>

<success_criteria>
- design-audit.md Section 15 calls generate-report.sh automatically after flow review
- Report path printed to terminal with branded SpSk box
- Report failure is non-blocking (warning only, audit results still available)
- Human has visually confirmed the report renders correctly in a browser
- Print-to-PDF produces a clean document
</success_criteria>

<output>
After completion, create `.planning/phases/06-html-diagnostic-report/06-02-SUMMARY.md`
</output>
