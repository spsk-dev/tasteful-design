---
phase: 01-scaffold-port-evals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - CLAUDE.md
  - README.md
  - LICENSE
  - VERSION
  - .gitignore
  - commands/design.md
  - commands/design-review.md
  - commands/design-improve.md
  - commands/design-validate.md
  - skills/design-review/SKILL.md
  - skills/design-review/references/typography.md
  - skills/design-review/references/color.md
  - skills/design-review/references/layout.md
  - skills/design-review/references/icons.md
  - skills/design-review/references/motion.md
  - skills/design-review/references/intent.md
  - skills/design-review/references/visual-design-rules.md
  - config/scoring.json
  - config/anti-slop.json
  - config/style-presets.json
  - config/design-system.example.json
  - hooks/hooks.json
  - scripts/suggest-review.sh
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02
  - SCAF-03
  - SCAF-04
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06
  - PORT-07
  - PORT-08
  - PORT-09
  - PORT-10

must_haves:
  truths:
    - "Plugin is recognized by Claude Code when installed (plugin.json valid)"
    - "All 4 slash commands (/design, /design-review, /design-improve, /design-validate) are registered"
    - "No hardcoded paths exist anywhere in the repo (no /Users/, no ~/.claude/plugins/design-review/)"
    - "References are loaded from skills/design-review/references/ not top-level references/"
    - "Hook fires on PostToolUse Write|Edit events"
    - "README has install command and demo GIF placeholder"
    - "CLAUDE.md documents all 4 commands with usage"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with metadata, version, description"
      contains: "\"name\""
    - path: "commands/design-review.md"
      provides: "8-specialist review command"
      min_lines: 700
    - path: "commands/design.md"
      provides: "Orchestrator routing to sub-commands"
      min_lines: 100
    - path: "skills/design-review/SKILL.md"
      provides: "Contextual knowledge trigger for design review"
      min_lines: 20
    - path: "config/scoring.json"
      provides: "Dimension weights and scoring thresholds"
      contains: "weight"
    - path: "hooks/hooks.json"
      provides: "PostToolUse hook definition"
      contains: "PostToolUse"
    - path: "README.md"
      provides: "GitHub showcase with install command"
      contains: "install-plugin"
    - path: "CLAUDE.md"
      provides: "Command documentation for plugin users"
      contains: "/design-review"
  key_links:
    - from: "commands/design-review.md"
      to: "skills/design-review/references/*.md"
      via: "${CLAUDE_PLUGIN_ROOT} path references"
      pattern: "CLAUDE_PLUGIN_ROOT.*references"
    - from: "hooks/hooks.json"
      to: "scripts/suggest-review.sh"
      via: "command path in hook definition"
      pattern: "CLAUDE_PLUGIN_ROOT.*suggest-review"
    - from: ".claude-plugin/plugin.json"
      to: "commands/"
      via: "Plugin discovery convention"
      pattern: "commands"
---

<objective>
Scaffold the SpSk repo structure with plugin manifest, port all 22 design-review plugin files from ~/.claude/plugins/design-review/ into the new structure, audit and fix all hardcoded paths, and create the README.md + CLAUDE.md documentation.

Purpose: Establish the installable plugin structure so that all subsequent work (docs, evals) has a working plugin to build on.
Output: A complete plugin directory that Claude Code can discover and load, with all 4 commands, config, hooks, references, and user-facing docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-port-evals/01-CONTEXT.md
@.planning/phases/01-scaffold-port-evals/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repo scaffold and supporting files</name>
  <files>
    .claude-plugin/plugin.json, VERSION, LICENSE, .gitignore,
    skills/design-review/SKILL.md,
    skills/design-review/references/typography.md, skills/design-review/references/color.md,
    skills/design-review/references/layout.md, skills/design-review/references/icons.md,
    skills/design-review/references/motion.md, skills/design-review/references/intent.md,
    skills/design-review/references/visual-design-rules.md,
    config/scoring.json, config/anti-slop.json, config/style-presets.json, config/design-system.example.json,
    hooks/hooks.json, scripts/suggest-review.sh
  </files>
  <read_first>
    ~/.claude/plugins/design-review/README.md
    ~/.claude/plugins/design-review/VERSION
    ~/.claude/plugins/design-review/config/scoring.json
    ~/.claude/plugins/design-review/config/anti-slop.json
    ~/.claude/plugins/design-review/config/style-presets.json
    ~/.claude/plugins/design-review/config/design-system.example.json
    ~/.claude/plugins/design-review/hooks/hooks.json
    ~/.claude/plugins/design-review/scripts/suggest-review.sh
    ~/.claude/plugins/design-review/references/typography.md
    ~/.claude/plugins/design-review/references/color.md
    ~/.claude/plugins/design-review/references/layout.md
    ~/.claude/plugins/design-review/references/icons.md
    ~/.claude/plugins/design-review/references/motion.md
    ~/.claude/plugins/design-review/references/intent.md
    ~/.claude/plugins/design-review/references/visual-design-rules.md
    ~/.claude/plugins/design-review/references/harness.md
    ~/.claude/plugins/design-review/references/workflows.md
    .planning/phases/01-scaffold-port-evals/01-CONTEXT.md
    .planning/phases/01-scaffold-port-evals/01-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Create directories**
    Create: `.claude-plugin/`, `commands/`, `skills/design-review/references/`, `config/`, `hooks/`, `scripts/`

    **Step 2: Create plugin manifest (per D-01)**
    Create `.claude-plugin/plugin.json` with fields:
    - `name`: "spsk" (or "design-review" -- Claude's discretion per CONTEXT.md)
    - `version`: read from source VERSION file (currently "1.0.0")
    - `description`: "AI-powered design review with 8 specialist agents, weighted scoring, and multi-tier degradation"
    - Follow official Anthropic plugin.json conventions exactly (inspect other plugins at ~/.claude/plugins/ for format reference)

    **Step 3: Port reference files (per D-03, D-06)**
    Copy from `~/.claude/plugins/design-review/references/` to `skills/design-review/references/`:
    - typography.md, color.md, layout.md, icons.md, motion.md, intent.md, visual-design-rules.md
    - NOTE: `harness.md` and `workflows.md` from source -- evaluate whether these belong in references or elsewhere. If they contain operational knowledge (how to run the harness, workflow steps), they may belong in the skill SKILL.md or as separate docs. Use judgment (Claude's discretion).

    **Step 4: Create skill file (per D-03)**
    Create `skills/design-review/SKILL.md` with YAML frontmatter:
    - `name`: "Design Review"
    - `description`: brief description of when/how design review activates
    - Body: overview of the 8-specialist review process, when to use it, reference to commands
    - If the source plugin has a monolithic SKILL.md or similar knowledge file, decompose it. Otherwise create from the design-review.md command's documentation sections.

    **Step 5: Port config files (per D-02)**
    Copy from `~/.claude/plugins/design-review/config/` to `config/`:
    - scoring.json, anti-slop.json, style-presets.json, design-system.example.json
    No path changes expected in config JSON files.

    **Step 6: Port hooks (per D-02)**
    Copy `~/.claude/plugins/design-review/hooks/hooks.json` to `hooks/hooks.json`
    Verify hooks.json uses `${CLAUDE_PLUGIN_ROOT}` (source already does per research).
    Update the hook command path in hooks.json to reference `${CLAUDE_PLUGIN_ROOT}/scripts/suggest-review.sh` (scripts/ stays at top level per D-02).

    **Step 7: Port hook script to top-level scripts/ (per D-02)**
    Copy `~/.claude/plugins/design-review/scripts/suggest-review.sh` to `scripts/suggest-review.sh`
    IMPORTANT: Per D-02, preserve source directory structure with scripts/ at top level. Do NOT move scripts under hooks/. The source has scripts/ as a top-level directory and that structure must be preserved.
    Make the script executable: `chmod +x scripts/suggest-review.sh`

    **Step 8: Create supporting files**
    - `VERSION`: "1.0.0"
    - `LICENSE`: MIT license with "Felipe Machado" and year 2026
    - `.gitignore`: Exclude `.design-reviews/`, `evals/results/*.tmp`, `.DS_Store`, `node_modules/`
  </action>
  <verify>
    <automated>
      test -f .claude-plugin/plugin.json && echo "plugin.json exists" &&
      test -f skills/design-review/SKILL.md && echo "SKILL.md exists" &&
      test -f config/scoring.json && echo "scoring.json exists" &&
      test -f hooks/hooks.json && echo "hooks.json exists" &&
      test -f scripts/suggest-review.sh && echo "suggest-review.sh exists" &&
      test -x scripts/suggest-review.sh && echo "suggest-review.sh is executable" &&
      test -f VERSION && echo "VERSION exists" &&
      test -f LICENSE && echo "LICENSE exists" &&
      grep -q "MIT" LICENSE && echo "LICENSE is MIT" &&
      jq empty .claude-plugin/plugin.json && echo "plugin.json valid JSON" &&
      jq empty config/scoring.json && echo "scoring.json valid JSON" &&
      jq empty config/anti-slop.json && echo "anti-slop.json valid JSON" &&
      jq empty config/style-presets.json && echo "style-presets.json valid JSON" &&
      REFS=$(ls skills/design-review/references/*.md 2>/dev/null | wc -l | tr -d ' ') &&
      test "$REFS" -ge 5 && echo "reference files: $REFS (>= 5)" &&
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.claude-plugin/plugin.json` exists and is valid JSON with `"name"` and `"version"` fields
    - `skills/design-review/SKILL.md` exists with YAML frontmatter (contains `---` on line 1)
    - 7 reference files exist in `skills/design-review/references/`
    - 4 config JSON files exist in `config/` and all pass `jq empty`
    - `hooks/hooks.json` contains `PostToolUse` and `${CLAUDE_PLUGIN_ROOT}`
    - `scripts/suggest-review.sh` exists at TOP LEVEL (per D-02) and is executable
    - VERSION contains "1.0.0"
    - LICENSE contains "MIT" and "Felipe Machado"
  </acceptance_criteria>
  <done>Repo scaffold created with plugin manifest, skill, references, config, hooks, and supporting files. scripts/ preserved at top level per D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Port command files and audit hardcoded paths</name>
  <files>
    commands/design.md, commands/design-review.md, commands/design-improve.md, commands/design-validate.md
  </files>
  <read_first>
    ~/.claude/plugins/design-review/commands/design.md
    ~/.claude/plugins/design-review/commands/design-review.md
    ~/.claude/plugins/design-review/commands/design-improve.md
    ~/.claude/plugins/design-review/commands/design-validate.md
    .planning/phases/01-scaffold-port-evals/01-CONTEXT.md
    .planning/phases/01-scaffold-port-evals/01-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Copy and adapt command files (per D-02, D-04)**
    Copy these files from `~/.claude/plugins/design-review/` preserving content:
    - `commands/design.md` -> `commands/design.md`
    - `commands/design-review.md` -> `commands/design-review.md`
    - `commands/design-improve.md` -> `commands/design-improve.md`
    - `commands/design-validate.md` -> `commands/design-validate.md`

    In EVERY copied file, search for and replace:
    - Any `~/.claude/plugins/design-review/` -> `${CLAUDE_PLUGIN_ROOT}/`
    - Any `/Users/felipemachado/` -> remove or replace with `${CLAUDE_PLUGIN_ROOT}/`
    - Any `references/` paths that should now point to `skills/design-review/references/` -- update the @-references accordingly

    **Step 2: Full hardcoded path audit (per D-04, PORT-08)**
    Run: `grep -rn '/Users/\|~/.claude/plugins/design-review\|/home/' .` across ALL files in the repo (exclude .git/ and .planning/).
    Fix every match. Zero tolerance -- any remaining hardcoded path is a blocker.
    The README.md from source has one known hardcoded path (line 13 with `~/.claude/plugins/design-review`).
  </action>
  <verify>
    <automated>
      test -f commands/design-review.md && echo "design-review.md exists" &&
      test -f commands/design.md && echo "design.md exists" &&
      test -f commands/design-improve.md && echo "design-improve.md exists" &&
      test -f commands/design-validate.md && echo "design-validate.md exists" &&
      LINES=$(wc -l < commands/design-review.md | tr -d ' ') &&
      test "$LINES" -ge 700 && echo "design-review.md line count: $LINES (>= 700)" &&
      LINES=$(wc -l < commands/design.md | tr -d ' ') &&
      test "$LINES" -ge 100 && echo "design.md line count: $LINES (>= 100)" &&
      HARDCODED=$(grep -rn '/Users/felipemachado\|~/.claude/plugins/design-review' --include='*.md' --include='*.json' --include='*.sh' . | grep -v '.planning/' | grep -v '.git/' | wc -l | tr -d ' ') &&
      test "$HARDCODED" -eq 0 && echo "No hardcoded paths found" &&
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 command files exist in `commands/` with line counts: design-review.md >= 700, design.md >= 100, design-improve.md >= 150, design-validate.md >= 150
    - `grep -rn '/Users/felipemachado\|~/.claude/plugins/design-review' . --include='*.md' --include='*.json' --include='*.sh' | grep -v '.planning/' | grep -v '.git/'` returns zero results
    - Command files use `${CLAUDE_PLUGIN_ROOT}` for all internal paths
    - Reference paths in commands point to `skills/design-review/references/`
  </acceptance_criteria>
  <done>All 4 command files ported with zero hardcoded paths. Full repo audit confirms no absolute path references remain.</done>
</task>

<task type="auto">
  <name>Task 3: Create README.md and CLAUDE.md documentation</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    ~/.claude/plugins/design-review/README.md
    ~/.claude/plugins/design-review/commands/design.md
    ~/.claude/plugins/design-review/commands/design-review.md
    ~/.claude/plugins/design-review/commands/design-improve.md
    ~/.claude/plugins/design-review/commands/design-validate.md
    .claude-plugin/plugin.json
    config/scoring.json
    .planning/phases/01-scaffold-port-evals/01-CONTEXT.md
    .context/intent.md
  </read_first>
  <action>
    **README.md (per D-14, SCAF-02)**
    Create a GitHub-facing README with these sections (professional but not corporate tone):
    1. **Title + tagline**: "SpSk -- Simple Skill" with one-line description of what it does
    2. **Demo GIF placeholder**: `<!-- Demo GIF will be added in Phase 2 -->` with a note like "30-second demo coming soon"
    3. **Install**: Exact command: `claude /install-plugin spsk@felipemachado/spsk`
    4. **Quick Start**: Show running `/design-review` on a page, what output looks like
    5. **Commands**: Table of all 4 commands with description and argument hints:
       - `/design` -- orchestrator (routes to review/improve/validate)
       - `/design-review [url] [--quick] [--ref url]` -- full 8-specialist review
       - `/design-improve [url]` -- iterative build/review/fix loop
       - `/design-validate [url]` -- functional validation via Playwright
    6. **Architecture overview**: Brief description of 8-specialist model, boss synthesizer, SHIP/BLOCK verdict, scoring. Link to ARCHITECTURE.md for details.
    7. **Degradation tiers**: Table showing Tier 1 (Claude + Gemini + Haiku), Tier 2 (Claude-only), Tier 3 (code analysis only)
    8. **The story**: Brief "40% to 100%" narrative -- v1 single-agent failed, iteration led to v4 multi-agent. Link to CHANGELOG.md.
    9. **Evals**: How to run `./evals/run-evals.sh`, what it proves
    10. **License**: MIT

    Do NOT include big ASCII art. Keep it clean and scannable.

    **CLAUDE.md (per D-15, SCAF-03)**
    Create plugin-user-facing CLAUDE.md documenting:
    1. **What this plugin does**: One paragraph overview
    2. **Available commands** with full usage for each:
       - `/design` with subcommands and routing logic
       - `/design-review [page-url] [--quick] [--ref url]` with all flags explained
       - `/design-improve [page-url]` with iteration behavior explained
       - `/design-validate [page-url]` with Playwright dependency noted
    3. **Configuration**: How to customize scoring.json, anti-slop.json, style-presets.json
    4. **Degradation behavior**: What happens when Gemini is unavailable
    5. **Quick mode**: What `--quick` does (4 specialists instead of 8, formula uses /13 weights)
  </action>
  <verify>
    <automated>
      grep -q "install-plugin" README.md && echo "README has install command" &&
      grep -q "design-review" README.md && echo "README mentions design-review" &&
      grep -q "ARCHITECTURE.md" README.md && echo "README links to ARCHITECTURE.md" &&
      grep -q "CHANGELOG.md" README.md && echo "README links to CHANGELOG.md" &&
      grep -q "40%" README.md && echo "README has failure story" &&
      grep -q "/design-review" CLAUDE.md && echo "CLAUDE.md has design-review" &&
      grep -q "/design-improve" CLAUDE.md && echo "CLAUDE.md has design-improve" &&
      grep -q "/design-validate" CLAUDE.md && echo "CLAUDE.md has design-validate" &&
      grep -q "/design" CLAUDE.md && echo "CLAUDE.md has design" &&
      grep -q "scoring.json" CLAUDE.md && echo "CLAUDE.md mentions config" &&
      grep -q "\-\-quick" CLAUDE.md && echo "CLAUDE.md documents quick mode" &&
      echo "ALL CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the string `claude /install-plugin spsk@felipemachado/spsk`
    - README.md contains "ARCHITECTURE.md" link and "CHANGELOG.md" link
    - README.md contains "40%" (failure story reference)
    - README.md contains "Tier 1", "Tier 2", "Tier 3" (degradation tiers)
    - README.md contains demo GIF placeholder comment
    - CLAUDE.md contains documentation for all 4 commands: `/design`, `/design-review`, `/design-improve`, `/design-validate`
    - CLAUDE.md contains `--quick` flag documentation
    - CLAUDE.md contains `scoring.json` reference
    - CLAUDE.md line count >= 80 (substantive documentation, not a stub)
  </acceptance_criteria>
  <done>README.md is a professional GitHub showcase with install command, architecture overview, failure story, and degradation tiers. CLAUDE.md documents all 4 commands with flags and configuration options.</done>
</task>

</tasks>

<verification>
- `jq empty .claude-plugin/plugin.json` exits 0
- `jq empty config/scoring.json` exits 0
- `jq empty config/anti-slop.json` exits 0
- `jq empty config/style-presets.json` exits 0
- All 4 command .md files exist in commands/ with frontmatter (line 1 is `---`)
- skills/design-review/SKILL.md exists with frontmatter
- 7 reference .md files exist in skills/design-review/references/
- hooks/hooks.json references `${CLAUDE_PLUGIN_ROOT}`
- scripts/suggest-review.sh exists at top level (per D-02) and is executable
- Zero hardcoded paths in any file (grep returns empty)
- LICENSE exists and contains "MIT"
- README.md has install command, CLAUDE.md has all 4 commands documented
</verification>

<success_criteria>
A complete, portable plugin structure exists that Claude Code can discover via .claude-plugin/plugin.json. All source files are ported with zero hardcoded paths. scripts/ directory preserved at top level per D-02. README.md and CLAUDE.md provide user-facing documentation. The plugin is ready for ARCHITECTURE.md/CHANGELOG.md (Plan 02) and eval harness (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-port-evals/01-01-SUMMARY.md`
</output>
