---
phase: 04-flow-navigation-engine
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - evals/fixtures/flow-test/index.html
  - evals/fixtures/flow-test/page2.html
  - evals/fixtures/flow-test/done.html
autonomous: false
requirements: [FLOW-07, FLOW-08]
must_haves:
  truths:
    - "User can provide --auth and the agent pauses for manual login before continuing the audit"
    - "Flow stops gracefully when the agent detects a completion/success state"
    - "Flow stops gracefully when no matching CTAs are found (dead end)"
    - "Partial flow state is preserved when flow ends early"
  artifacts:
    - path: "evals/fixtures/flow-test/index.html"
      provides: "3-page test fixture for flow audit smoke testing"
      contains: "Get Started"
  key_links:
    - from: "commands/design-audit.md"
      to: "user interaction"
      via: "checkpoint pause for auth login"
      pattern: "login.*continue"
---

<objective>
Create test fixtures and verify the flow navigation engine works end-to-end, including auth flow and graceful stop behavior.

Purpose: FLOW-07 (auth) and FLOW-08 (graceful stop) require verification against real browser behavior. Plan 02 writes the command; this plan creates test fixtures and verifies the complete flow.
Output: Test fixtures + human-verified working flow navigation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-flow-navigation-engine/04-CONTEXT.md
@.planning/phases/04-flow-navigation-engine/04-02-SUMMARY.md

@commands/design-audit.md
@config/flow-scoring.json
@skills/design-review/references/flow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a minimal test fixture and run smoke test</name>
  <files>evals/fixtures/flow-test/index.html, evals/fixtures/flow-test/page2.html, evals/fixtures/flow-test/done.html</files>
  <read_first>commands/design-audit.md</read_first>
  <action>
Create a minimal 3-page static HTML test fixture for flow audit smoke testing:

**evals/fixtures/flow-test/index.html:**
- A simple page with heading "Welcome" and a button "Get Started" linking to page2.html
- Inline CSS for basic styling (no external deps)

**evals/fixtures/flow-test/page2.html:**
- Heading "Step 2: Details" with a form-like layout
- A "Continue" button linking to done.html

**evals/fixtures/flow-test/done.html:**
- Heading "All Done!" with confirmation text "You have completed the flow"
- No outbound CTAs (this IS the dead end / success state)

These are static HTML files -- no JS framework, no build step. They test:
- FLOW-01: Basic navigation from URL
- FLOW-03: Screenshot at each page
- FLOW-06: Can be used with --steps index.html,page2.html,done.html
- FLOW-08: Agent should detect "All Done!" as completion state

Serve them locally for testing: `npx serve evals/fixtures/flow-test -p 3456`
  </action>
  <verify>
    <automated>test -f evals/fixtures/flow-test/index.html && test -f evals/fixtures/flow-test/page2.html && test -f evals/fixtures/flow-test/done.html && grep -q "Get Started" evals/fixtures/flow-test/index.html && grep -q "All Done" evals/fixtures/flow-test/done.html && echo "Fixtures OK"</automated>
  </verify>
  <done>Three static HTML files exist forming a simple 3-screen flow: Welcome > Details > Done. Each has heading text and a CTA to the next page (except the final page which has completion text).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify flow navigation engine end-to-end</name>
  <action>
Human verification of the complete flow navigation engine. The executor presents the test steps below and waits for user confirmation.
  </action>
  <verify>User types "approved"</verify>
  <done>Human confirms flow navigation works in both intent and deterministic modes, with graceful completion detection.</done>
  <what-built>
Flow navigation engine (design-audit command) with:
- Intent-driven navigation via Playwright MCP
- Deterministic --steps mode
- Screenshot capture at each screen
- Progressive flow-state.json
- Auth checkpoint support
- Graceful completion/dead-end detection
- 3-page test fixture for smoke testing
  </what-built>
  <how-to-verify>
1. Start the test fixture server: `npx serve evals/fixtures/flow-test -p 3456`
2. Ensure Playwright MCP is registered: `claude mcp add playwright -- npx @playwright/mcp@latest --viewport-size 1440x900` (if not already)
3. Test deterministic mode: `/design-audit http://localhost:3456 --steps http://localhost:3456/index.html,http://localhost:3456/page2.html,http://localhost:3456/done.html`
   - Expected: 3 screenshots captured, flow-state.json in /tmp/design-audit-*/
4. Test intent mode: `/design-audit http://localhost:3456 --flow "complete the signup flow"`
   - Expected: Agent navigates Welcome > Details > Done, captures screenshots, detects "All Done!" as completion
5. Test max-screens: `/design-audit http://localhost:3456 --flow "explore everything" --max-screens 2`
   - Expected: Only 2 screens captured, status "max_screens_reached"
6. Verify flow-state.json: `cat /tmp/design-audit-*/flow-state.json | python3 -m json.tool`
   - Expected: Valid JSON with version, status, screens array with number/name/slug/url/screenshot_path/timestamp fields
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Test fixtures exist and have correct structure
2. `/design-audit` command runs successfully in both modes
3. Screenshots captured in /tmp/design-audit-*/ directory
4. flow-state.json is valid JSON matching the contract schema
5. Flow stops at "All Done!" page (FLOW-08)
</verification>

<success_criteria>
- 3-page test fixture serves correctly on localhost
- Deterministic mode captures all 3 pages
- Intent mode navigates the flow by clicking CTAs
- flow-state.json has correct schema and progressive persistence
- Max-screens limit is respected
- Human verifies the complete flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-flow-navigation-engine/04-03-SUMMARY.md`
</output>
