---
phase: 13-few-shot-examples-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design-review/prompts/font.md
  - skills/design-review/prompts/color.md
  - skills/design-review/prompts/layout.md
  - skills/design-review/prompts/icons.md
  - skills/design-review/prompts/motion.md
  - skills/design-review/prompts/code-a11y.md
  - skills/design-review/prompts/intent.md
  - skills/design-review/prompts/boss.md
autonomous: true
requirements: [PRMT-04, PRMT-05]

must_haves:
  truths:
    - "Every specialist prompt file contains an <examples> section with 2-3 curated examples"
    - "Each example set covers different score levels (a score-2 example, a score-3 example, optionally a score-4 example)"
    - "Complex specialists (Intent, Layout, Boss) have explicit <thinking> + <answer> separation with structured reasoning guidance"
    - "Simple specialists (Font, Color, Icons, Motion, Code/A11y) have examples but no additional CoT changes"
    - "All existing <specialist_output> and <boss_output> JSON schemas are preserved unchanged"
  artifacts:
    - path: "skills/design-review/prompts/font.md"
      provides: "Typography few-shot examples at 3 score levels"
      contains: "<examples>"
    - path: "skills/design-review/prompts/color.md"
      provides: "Color few-shot examples at 3 score levels"
      contains: "<examples>"
    - path: "skills/design-review/prompts/layout.md"
      provides: "Layout few-shot examples + enhanced CoT reasoning"
      contains: "<examples>"
    - path: "skills/design-review/prompts/icons.md"
      provides: "Icons few-shot examples at 3 score levels"
      contains: "<examples>"
    - path: "skills/design-review/prompts/motion.md"
      provides: "Motion few-shot examples at 3 score levels"
      contains: "<examples>"
    - path: "skills/design-review/prompts/code-a11y.md"
      provides: "Code/A11y few-shot examples at 3 score levels"
      contains: "<examples>"
    - path: "skills/design-review/prompts/intent.md"
      provides: "Intent few-shot examples + enhanced CoT with 4 sub-scores"
      contains: "<examples>"
    - path: "skills/design-review/prompts/boss.md"
      provides: "Boss synthesizer few-shot examples + enhanced CoT for cross-specialist reasoning"
      contains: "<examples>"
  key_links:
    - from: "skills/design-review/prompts/*.md"
      to: "commands/design-review.md"
      via: "@ includes"
      pattern: "@.*prompts/"
---

<objective>
Add 2-3 curated few-shot examples to all 8 specialist prompt files and enhance chain-of-thought separation for the 3 complex specialists (Intent, Layout, Boss).

Purpose: Few-shot examples are the single most reliable way to steer Claude's output format and scoring calibration. Without them, specialists guess at what "good output" looks like. Chain-of-thought separation for complex specialists forces structured reasoning before committing to scores, improving calibration for multi-dimensional evaluations.

Output: 8 updated prompt files with <examples> sections and enhanced CoT guidance where appropriate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-extraction-restructuring/08-01-SUMMARY.md
@.planning/phases/10-structured-json-output/10-01-SUMMARY.md

@skills/design-review/prompts/font.md
@skills/design-review/prompts/color.md
@skills/design-review/prompts/layout.md
@skills/design-review/prompts/icons.md
@skills/design-review/prompts/motion.md
@skills/design-review/prompts/code-a11y.md
@skills/design-review/prompts/intent.md
@skills/design-review/prompts/boss.md

<interfaces>
<!-- Current prompt structure (established Phase 08, updated Phase 10): -->
<!-- Each specialist prompt follows: <role> -> <reference_knowledge> -> <instructions> -> <scoring_rubric> -> <output_format> -->
<!-- Phase 10 added <thinking> instruction + <specialist_output> JSON wrapper to output_format -->
<!-- Examples go AFTER output_format as a new <examples> section at the end of each file -->

<!-- JSON output schemas (must be preserved exactly): -->

Single-score specialist output (font, color, layout, icons, motion, code_a11y):
```json
{
  "specialist": "typography",
  "score": 3,
  "findings": [{ "element": "...", "issue": "...", "recommendation": "..." }],
  "summary": "one-line summary"
}
```

Multi-score specialist output (intent):
```json
{
  "specialist": "intent",
  "scores": { "intent_match": 3, "originality": 2, "ux_flow": 3, "copy_quality": 3 },
  "findings": [{ "dimension": "...", "element": "...", "issue": "...", "recommendation": "..." }],
  "summary": "one-line summary"
}
```

Boss output (after human-readable markdown):
```json
{
  "page_name": "...",
  "page_type": "...",
  "mode": "full",
  "tier": 1,
  "scores": { ... },
  "weighted_score": 2.69,
  "verdict": "CONDITIONAL",
  "consensus_findings": [...],
  "top_fixes": [...],
  "what_works": [...],
  "gold_standard_gap": "..."
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add few-shot examples to 5 simple specialists (Font, Color, Icons, Motion, Code/A11y)</name>
  <files>
    skills/design-review/prompts/font.md
    skills/design-review/prompts/color.md
    skills/design-review/prompts/icons.md
    skills/design-review/prompts/motion.md
    skills/design-review/prompts/code-a11y.md
  </files>
  <read_first>
    skills/design-review/prompts/font.md
    skills/design-review/prompts/color.md
    skills/design-review/prompts/icons.md
    skills/design-review/prompts/motion.md
    skills/design-review/prompts/code-a11y.md
    skills/design-review/references/typography.md
    skills/design-review/references/icons.md
    skills/design-review/references/motion.md
    config/anti-slop.json
  </read_first>
  <action>
Append an `<examples>` section at the END of each of the 5 simple specialist prompt files (after the closing `</output_format>` tag). Each `<examples>` section contains 2-3 `<example>` blocks showing curated ideal output at different score levels.

Do NOT modify any existing content above the `</output_format>` closing tag. The examples are purely additive.

**Example design principles (per CONTEXT.md):**
- Each example shows a different score level (e.g., score 2 and score 3, or score 2, score 3, and score 4)
- Examples are curated from realistic review scenarios, not randomly generated
- Each example includes the full JSON output matching the specialist's schema exactly
- Examples show proper use of element selectors, specific issues, and actionable recommendations
- Keep each example concise: 1-2 findings per example (not 5), to limit token growth

**Per-specialist example content:**

**font.md** -- 2 examples:
- Score 2 example: SaaS landing page using Inter for everything, basic hierarchy but no craft. Findings reference the scoring rubric's "Default/generic fonts" anchor.
- Score 4 example: Editorial page using Instrument Serif + DM Sans pairing, correct line-height scaling, proper tracking. Findings reference "Distinctive, well-paired fonts."

**color.md** -- 2 examples:
- Score 2 example: Dashboard with pure #000 background, no tinted neutrals, generic blue accent. References "Generic palette with no personality."
- Score 3 example: Marketing page with cohesive warm palette, WCAG AA compliant, clear 60/30/10 but slightly generic neutral tone. References "Cohesive palette matching page mood."

**icons.md** -- 2 examples:
- Score 1 example: Page mixing Heroicons, Font Awesome, and emoji as functional icons. References "Mixed icon libraries, emoji as functional icons."
- Score 3 example: Page using Lucide consistently with one missing aria-label on an icon-only button. References "Consistent library and sizing, proper semantics, minor gaps."

**motion.md** -- 2 examples:
- Score 2 example: Page with animate-bounce on a CTA button, linear easing on hover transitions, missing prefers-reduced-motion. References "Basic transitions exist but linear easing."
- Score 3 example: Page with proper ease-out transitions (200ms), prefers-reduced-motion media query present, one slightly long animation (800ms entrance). References "Appropriate durations with proper easing."

**code-a11y.md** -- 2 examples:
- Score 2 example: Page with semantic HTML but missing focus styles, color-only status indicators, no skip link. References "Some accessibility but gaps."
- Score 4 example: Page with full semantic HTML, CSS custom properties, skip link, focus-visible styles, proper aria-labels, mobile-first responsive. References "Full semantic HTML, complete accessibility."

**Format for each example block:**
```
<example>
<scenario>Brief description of what page is being reviewed</scenario>
<specialist_output>
{
  "specialist": "...",
  "score": N,
  "findings": [...],
  "summary": "..."
}
</specialist_output>
</example>
```

Note: Do NOT include a `<thinking>` block in the examples for these simple specialists -- only show the `<specialist_output>` JSON. The thinking is done at inference time, not demonstrated in examples.
  </action>
  <verify>
    <automated>grep -c '<examples>' skills/design-review/prompts/font.md skills/design-review/prompts/color.md skills/design-review/prompts/icons.md skills/design-review/prompts/motion.md skills/design-review/prompts/code-a11y.md | grep -v ':0$' | wc -l | grep -q '5' && echo "PASS: All 5 simple specialists have examples" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep '<examples>' skills/design-review/prompts/font.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/color.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/icons.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/motion.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/code-a11y.md returns 1 match
    - grep '<example>' in each file returns 2-3 matches (2-3 examples per specialist)
    - grep '<specialist_output>' count in each file is at least 3 (1 original + 2 in examples)
    - No existing content above </output_format> is modified (diff shows only additions at end of file)
  </acceptance_criteria>
  <done>Each of the 5 simple specialist prompt files has an <examples> section appended at the end containing 2-3 curated examples at different score levels, with correct JSON schema matching the specialist's output format.</done>
</task>

<task type="auto">
  <name>Task 2: Add few-shot examples + enhanced CoT to 3 complex specialists (Intent, Layout, Boss)</name>
  <files>
    skills/design-review/prompts/intent.md
    skills/design-review/prompts/layout.md
    skills/design-review/prompts/boss.md
  </files>
  <read_first>
    skills/design-review/prompts/intent.md
    skills/design-review/prompts/layout.md
    skills/design-review/prompts/boss.md
    skills/design-review/references/intent.md
    skills/design-review/references/layout.md
    config/scoring.json
  </read_first>
  <action>
Two changes per file for these complex specialists: (A) enhance the CoT guidance in the existing `<output_format>` section, and (B) append an `<examples>` section at the end.

**A. Enhanced CoT guidance (per PRMT-05):**

The prompts already say "First, analyze ... in <thinking> tags." Enhance this to be more structured:

For **intent.md**: Update the output_format's thinking instruction to: "In your <thinking> section, reason through each of the four dimensions explicitly: (1) Does the visual tone match the content's purpose? (2) What AI-default patterns are present vs what is genuinely designed? (3) Can the user find and complete the primary action? (4) Is the copy specific, correct, and tone-appropriate? Then assign scores based on your reasoning." Do NOT change any other part of output_format.

For **layout.md**: Update the output_format's thinking instruction to: "In your <thinking> section, reason through: (1) Is spacing based on a consistent system (4px/8px grid)? (2) Compare desktop vs mobile -- what breaks or degrades? (3) Do sections vary in visual rhythm or repeat monotonously? Then assign a score based on your reasoning." Do NOT change any other part of output_format.

For **boss.md**: The boss prompt is structurally different (orchestrator protocol). Add a thinking instruction at the start of the `<output_format>` section: "Before generating the review, reason in <thinking> tags: (1) Which issues appear in 2+ specialist outputs? These are HIGH confidence. (2) Which specialist scores are outliers vs the group? Flag for scrutiny. (3) What is the weighted score calculation? Show the math. Then generate the review." Do NOT change the existing markdown template or JSON schema in boss.md.

**B. Append <examples> section:**

**intent.md** -- 3 examples (complex specialist deserves more coverage):
- Score pattern {2, 2, 2, 2}: Page using AI defaults everywhere (Inter font, purple gradient, three-column icon grid), buried CTA, generic "Learn More" button text. Show the multi-score output with intent_match, originality, ux_flow, copy_quality all scoring 2.
- Score pattern {3, 3, 3, 3}: Well-designed SaaS dashboard with clear intent, some originality, good UX flow, correct copy. Minor: one generic phrase in subheading.
- Score pattern {4, 2, 3, 3}: Emotionally resonant personal portfolio with strong intent match and good copy, but relies on Bootstrap grid (low originality) and has slightly unclear project navigation.

**layout.md** -- 2 examples:
- Score 2: Landing page with inconsistent gaps (16px, 24px, 40px mixed), no responsive breakpoints, wall-of-cards monotony. References "Basic responsive breakpoints but no fluid units."
- Score 4: Documentation site with 8px-grid spacing, fluid responsive with clamp(), varied section rhythm alternating between wide and narrow content. References "8px-grid spacing throughout."

**boss.md** -- 2 examples (show abbreviated versions -- boss output is large):
- CONDITIONAL verdict example: Show specialist scores array (e.g., Intent 3, Originality 2, UX 3, Typography 3, Color 2, Layout 3, Icons 2, Motion 3, Code 2), weighted calculation yielding 2.56/4.0, one consensus finding (2+ specialists), and top 3 fixes. Page type "landing" with threshold 3.0.
- SHIP verdict example: Show higher scores (Intent 4, Originality 3, UX 3, Typography 3, Color 3, Layout 3, Icons 3, Motion 3, Code 3), weighted calculation yielding 3.13/4.0, page type "landing." Show what_works with earned praise, gold_standard_gap noting the remaining gap.

For boss examples, show BOTH the abbreviated human-readable markdown AND the `<boss_output>` JSON, since the boss produces dual output. Keep the markdown portion brief (scores table + verdict + top 3 fixes only -- omit the full sections like "Cross-Specialist Findings" and "What Works" to limit token growth).

**Format for complex specialist examples:**
```
<example>
<scenario>Brief description</scenario>
<thinking>
[2-3 sentences of structured reasoning following the enhanced CoT pattern]
</thinking>
<specialist_output>
{ ... JSON ... }
</specialist_output>
</example>
```

For boss examples:
```
<example>
<scenario>Brief description</scenario>
<thinking>
[Cross-specialist agreement analysis, outlier detection, score calculation]
</thinking>
[Abbreviated markdown review]
<boss_output>
{ ... JSON ... }
</boss_output>
</example>
```
  </action>
  <verify>
    <automated>grep -c '<examples>' skills/design-review/prompts/intent.md skills/design-review/prompts/layout.md skills/design-review/prompts/boss.md | grep -v ':0$' | wc -l | grep -q '3' && echo "PASS: All 3 complex specialists have examples" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep '<examples>' skills/design-review/prompts/intent.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/layout.md returns 1 match
    - grep '<examples>' skills/design-review/prompts/boss.md returns 1 match
    - grep '<thinking>' in intent.md examples returns at least 3 (1 original instruction + 3 in examples)
    - grep 'reason through' in intent.md output_format section confirms enhanced CoT guidance
    - grep 'reason through' in layout.md output_format section confirms enhanced CoT guidance
    - grep 'reason in <thinking>' in boss.md output_format section confirms enhanced CoT guidance
    - grep '<boss_output>' in boss.md returns at least 3 (1 original schema + 2 in examples)
    - Intent examples show the 4-score pattern (intent_match, originality, ux_flow, copy_quality)
    - All existing JSON schemas in output_format sections are unchanged
  </acceptance_criteria>
  <done>Intent, Layout, and Boss prompt files each have enhanced CoT reasoning guidance in their output_format section AND an appended <examples> section with curated examples showing <thinking> blocks and correct JSON output at different score/verdict levels.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. All 8 prompt files contain `<examples>` sections:
   ```
   grep -l '<examples>' skills/design-review/prompts/*.md | wc -l
   # Expected: 8
   ```

2. Total example count across all files (18-22 expected: 5 simple x 2 + 2 complex x 2-3 + boss x 2):
   ```
   grep -c '<example>' skills/design-review/prompts/*.md
   ```

3. Existing structural validation still passes:
   ```
   bash evals/validate-structure.sh
   ```

4. No JSON schema changes in output_format sections (diff check):
   ```
   git diff skills/design-review/prompts/ | grep -c '"specialist"'
   # Should show additions only (in examples), not modifications to original schemas
   ```
</verification>

<success_criteria>
- All 8 specialist prompt files contain <examples> sections with 2-3 curated examples each
- Examples cover different score levels per specialist (not all score-3)
- Complex specialists (Intent, Layout, Boss) have enhanced <thinking> instructions in output_format
- No existing prompt structure (role, reference_knowledge, instructions, scoring_rubric, output_format) is modified
- JSON output schemas are preserved exactly as established in Phase 10
- validate-structure.sh passes (no structural regression)
</success_criteria>

<output>
After completion, create `.planning/phases/13-few-shot-examples-polish/13-01-SUMMARY.md`
</output>
