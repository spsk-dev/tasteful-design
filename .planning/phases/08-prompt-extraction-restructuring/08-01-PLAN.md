---
phase: 08-prompt-extraction-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design-review/prompts/font.md
  - skills/design-review/prompts/color.md
  - skills/design-review/prompts/layout.md
  - skills/design-review/prompts/icons.md
  - skills/design-review/prompts/motion.md
  - skills/design-review/prompts/intent.md
  - skills/design-review/prompts/copy.md
  - skills/design-review/prompts/code-a11y.md
  - skills/design-review/prompts/boss.md
autonomous: true
requirements:
  - PRMT-01
  - PRMT-02
  - PRMT-03
  - PRMT-06
  - PRMT-07

must_haves:
  truths:
    - "9 specialist prompt files exist in skills/design-review/prompts/"
    - "Every specialist prompt uses XML-structured sections (role, reference_knowledge, instructions, scoring_rubric, output_format)"
    - "Every specialist has a 4-level scoring rubric with concrete domain-specific anchors"
    - "No prompt file contains FLAG SPECIFICALLY, Find at least N, NEVER, or ALL-CAPS emphasis directives"
    - "Boss synthesizer prompt contains scoring formula, verdict rules, and output format in XML structure"
  artifacts:
    - path: "skills/design-review/prompts/font.md"
      provides: "Typography specialist prompt"
      contains: "<role>"
    - path: "skills/design-review/prompts/color.md"
      provides: "Color specialist prompt (Gemini-compatible)"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/layout.md"
      provides: "Layout specialist prompt (Gemini-compatible)"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/icons.md"
      provides: "Icons specialist prompt"
      contains: "<instructions>"
    - path: "skills/design-review/prompts/motion.md"
      provides: "Motion specialist prompt"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/intent.md"
      provides: "Intent/Originality/UX specialist prompt with 3 sub-scores"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/copy.md"
      provides: "Copy & Language specialist prompt"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/code-a11y.md"
      provides: "Code & Accessibility specialist prompt"
      contains: "<scoring_rubric>"
    - path: "skills/design-review/prompts/boss.md"
      provides: "Boss synthesizer protocol with scoring formula and verdict rules"
      contains: "<scoring_formula>"
  key_links:
    - from: "skills/design-review/prompts/font.md"
      to: "skills/design-review/references/typography.md"
      via: "reference_knowledge tag pointing to reference file"
      pattern: "references/typography\\.md"
    - from: "skills/design-review/prompts/color.md"
      to: "skills/design-review/references/color.md"
      via: "reference_knowledge tag"
      pattern: "references/color\\.md"
    - from: "skills/design-review/prompts/boss.md"
      to: "config/scoring.json"
      via: "scoring_formula section referencing weights"
      pattern: "scoring\\.json"
---

<objective>
Extract all 8 specialist prompts and the boss synthesizer from inline blocks in commands/design-review.md into individual prompt files under skills/design-review/prompts/. Each prompt is restructured with XML-tagged sections following Anthropic Claude 4.6 best practices, given a 4-level scoring rubric with concrete domain-specific anchors, and cleaned of over-aggressive directives.

Purpose: Enables isolated testing, smaller diffs per prompt change, and lays the foundation for eval-driven prompt iteration in later phases.
Output: 9 prompt files (font.md, color.md, layout.md, icons.md, motion.md, intent.md, copy.md, code-a11y.md, boss.md)
</objective>

<execution_context>
@/Users/felipemachado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/felipemachado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-extraction-restructuring/08-CONTEXT.md
@.planning/phases/08-prompt-extraction-restructuring/08-RESEARCH.md
@commands/design-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 specialist prompt files with XML structure, rubrics, and directive cleanup</name>
  <files>
    skills/design-review/prompts/font.md
    skills/design-review/prompts/color.md
    skills/design-review/prompts/layout.md
    skills/design-review/prompts/icons.md
    skills/design-review/prompts/motion.md
    skills/design-review/prompts/intent.md
    skills/design-review/prompts/copy.md
    skills/design-review/prompts/code-a11y.md
  </files>
  <read_first>
    commands/design-review.md (lines 319-548 contain all 8 specialist prompts -- the extraction source)
    .planning/phases/08-prompt-extraction-restructuring/08-RESEARCH.md (contains the XML template, all rubric anchors, directive cleanup mapping, Gemini considerations)
    skills/design-review/references/typography.md (font specialist reference knowledge -- verify reference path is correct)
    skills/design-review/references/color.md (color specialist reference)
    skills/design-review/references/layout.md (layout specialist reference)
    skills/design-review/references/icons.md (icons specialist reference)
    skills/design-review/references/motion.md (motion specialist reference)
    skills/design-review/references/intent.md (intent specialist reference)
  </read_first>
  <action>
Create the directory `skills/design-review/prompts/` and write 8 specialist prompt files.

Each file follows this exact XML skeleton (per PRMT-01):

```
<role>
You are a {domain} specialist evaluating frontend design quality.
You have deep expertise in {domain-specific areas from current prompt}.
</role>

<reference_knowledge>
Read: ${CLAUDE_PLUGIN_ROOT}/skills/design-review/references/{domain}.md
</reference_knowledge>

<instructions>
Evaluate {domain quality dimension} for this page.

Process:
1. Read the reference knowledge above
2. Examine the screenshots and source files
3. Identify 2-5 specific issues (each must name the element, its current state, and what it should be)
4. Evaluate whether your domain serves the page intent -- not in isolation
5. Score using the rubric below

Review checklist:
{converted from current inline REVIEW section -- preserve all check items}

Check for and flag:
{converted from current FLAG SPECIFICALLY blocks -- same items, normal case}
</instructions>

<scoring_rubric>
Score {domain} on a 1-4 scale:

- 1 (Poor): {domain-specific anchor from RESEARCH.md}
- 2 (Below Average): {domain-specific anchor from RESEARCH.md}
- 3 (Good): {domain-specific anchor from RESEARCH.md}
- 4 (Excellent): {domain-specific anchor from RESEARCH.md}

Each score must be justified by specific evidence from the page.
</scoring_rubric>

<output_format>
Return: issues list with specific elements + score + one-line summary.
</output_format>
```

**Directive cleanup rules (per PRMT-03):**
- "FLAG SPECIFICALLY:" becomes "Check for and flag:" in the instructions section
- "Find at least 2 issues." becomes part of the process step: "Identify 2-5 specific issues"
- "Find at least 3 issues (1+ per dimension)." becomes: "Identify 3-5 issues across your three dimensions. Cover each dimension."
- "ALL CAPS must have +0.05em tracking" becomes "uppercase text should have +0.05em tracking" (the CSS concept ALL CAPS is fine when literally referring to text-transform: uppercase, but do not use ALL-CAPS as emphasis)
- Remove bare "Score: Typography Quality 1-4." lines -- the scoring_rubric section replaces these

**Specialist-specific details:**

1. **font.md** -- Source: design-review.md lines 319-343. Model: Claude Sonnet. Reference: typography.md. Rubric anchors from RESEARCH.md "Font Rubric Anchors" section. The reference_knowledge tag points to `${CLAUDE_PLUGIN_ROOT}/skills/design-review/references/typography.md`.

2. **color.md** -- Source: lines 348-372. Model: Gemini CLI. Reference: color.md. CRITICAL: This prompt is passed as a `-p "..."` string to Gemini. It must read naturally even if XML tags are ignored. The reference_knowledge tag should say "Read: .color-reference.md" (the workspace copy, not the plugin path) because Gemini cannot access ${CLAUDE_PLUGIN_ROOT}. Rubric anchors from RESEARCH.md "Color Rubric Anchors".

3. **layout.md** -- Source: lines 376-399. Model: Gemini CLI. Same Gemini constraint as color. Reference_knowledge says "Read: .layout-reference.md". Rubric from "Layout Rubric Anchors".

4. **icons.md** -- Source: lines 403-427. Model: Claude Sonnet. No dedicated reference file exists (icons.md in references/ is the domain knowledge). Reference: icons.md. Rubric from "Icons Rubric Anchors".

5. **motion.md** -- Source: lines 430-452. Model: Claude Sonnet. Reference: motion.md. Note: this specialist reads source code ONLY (cannot see animations in screenshots). Reflect this in the role and instructions. Rubric from "Motion Rubric Anchors".

6. **intent.md** -- Source: lines 455-497. Model: Claude Sonnet. Reference: intent.md. This specialist returns THREE scores (Intent Match, Originality, UX Flow) not one. The scoring_rubric must have three sub-sections with anchors for each. Rubric from "Intent/Originality/UX Rubric Anchors". The output_format must specify "THREE scores + issues list + one-line summary each".

7. **copy.md** -- Source: lines 499-523. Model: Claude Haiku. No reference file exists for copy. Omit the reference_knowledge tag entirely. Rubric from "Copy Rubric Anchors".

8. **code-a11y.md** -- Source: lines 525-548. Model: Claude Sonnet. No reference file. Omit reference_knowledge tag. Rubric from "Code/A11y Rubric Anchors".

**Preserving behavior:** Compare each extracted prompt side-by-side with the source. Every review checklist item in the current prompt must appear in the instructions section. Every item in FLAG SPECIFICALLY must appear in "Check for and flag:". Do not add new review criteria or remove existing ones -- this is extraction, not rewriting.
  </action>
  <verify>
    <automated>
for f in font.md color.md layout.md icons.md motion.md intent.md copy.md code-a11y.md; do
  test -f "skills/design-review/prompts/$f" || { echo "MISSING: $f"; exit 1; }
  grep -q '<role>' "skills/design-review/prompts/$f" || { echo "MISSING <role> in $f"; exit 1; }
  grep -q '<instructions>' "skills/design-review/prompts/$f" || { echo "MISSING <instructions> in $f"; exit 1; }
  grep -q '<scoring_rubric>' "skills/design-review/prompts/$f" || { echo "MISSING <scoring_rubric> in $f"; exit 1; }
  grep -q '<output_format>' "skills/design-review/prompts/$f" || { echo "MISSING <output_format> in $f"; exit 1; }
  grep -q '1 (Poor)' "skills/design-review/prompts/$f" || { echo "MISSING rubric level 1 in $f"; exit 1; }
  grep -q '4 (Excellent)' "skills/design-review/prompts/$f" || { echo "MISSING rubric level 4 in $f"; exit 1; }
done
echo "All 8 specialist prompts have required XML tags and rubric levels"
# Verify no aggressive directives
AGGRESSIVE=$(grep -rn 'FLAG SPECIFICALLY\|Find at least [0-9]\|\bNEVER\b' skills/design-review/prompts/ 2>/dev/null || true)
test -z "$AGGRESSIVE" || { echo "AGGRESSIVE DIRECTIVES FOUND: $AGGRESSIVE"; exit 1; }
echo "No aggressive directives found"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist in skills/design-review/prompts/
    - Each file contains XML tags: `<role>`, `<instructions>`, `<scoring_rubric>`, `<output_format>`
    - Each file contains 4-level rubric with "1 (Poor)", "2 (Below Average)", "3 (Good)", "4 (Excellent)" and domain-specific text after each level
    - grep -rn "FLAG SPECIFICALLY" skills/design-review/prompts/ returns empty
    - grep -rn "Find at least [0-9]" skills/design-review/prompts/ returns empty
    - font.md contains "references/typography.md" in reference_knowledge tag
    - color.md contains ".color-reference.md" (Gemini workspace path, NOT ${CLAUDE_PLUGIN_ROOT})
    - layout.md contains ".layout-reference.md" (Gemini workspace path)
    - intent.md contains three score sections: "Intent Match", "Originality", "UX Flow"
    - copy.md and code-a11y.md do NOT contain a reference_knowledge tag (no reference file for these)
    - motion.md instructions mention "code-only" or "source code" analysis (cannot see animations in screenshots)
  </acceptance_criteria>
  <done>8 specialist prompt files created with XML structure, 4-level rubrics with concrete anchors, and all aggressive directives replaced with normal-case guidance</done>
</task>

<task type="auto">
  <name>Task 2: Create boss synthesizer prompt file with scoring protocol and verdict rules</name>
  <files>skills/design-review/prompts/boss.md</files>
  <read_first>
    commands/design-review.md (lines 552-648 contain the boss synthesis logic -- Phase 3)
    .planning/phases/08-prompt-extraction-restructuring/08-RESEARCH.md (Pattern 4: Boss Synthesizer Extraction)
    config/scoring.json (current scoring weights -- the boss references these)
  </read_first>
  <action>
Create `skills/design-review/prompts/boss.md` containing the boss synthesizer protocol.

CRITICAL DISTINCTION: The boss is NOT a separate agent. The orchestrator IS the boss. This file contains the synthesis protocol that the orchestrator reads and follows directly. It is NOT a subagent prompt like the 8 specialists.

Structure the file with these XML sections:

```
<role>
You are the boss designer synthesizer. You do NOT re-evaluate the design.
You trust the specialist findings and merge them into a coherent verdict.
</role>

<instructions>
After all specialists return their findings, synthesize following this protocol:

1. Cross-specialist agreement
   - Issues found by 2+ specialists get HIGH confidence
   - Flag these prominently in the output
   - Single-specialist issues get MEDIUM confidence

2. Deduplication
   - Same issue from multiple specialists: merge into one entry
   - Keep the most specific description
   - Note which specialists independently found it

3. Compute weighted score using the formula below

4. Apply context-aware verdict using page-type thresholds below

5. Generate prioritized fix list (top 5) ordered by:
   - Cross-specialist agreement (2+ specialists = higher priority)
   - Weight of affected dimension (Intent 3x > Color 2x > Icons 1x)
   - Severity (structural > cosmetic)
</instructions>

<scoring_formula>
Full mode (8 specialists, 10 scored dimensions):
(Intent*3 + Originality*3 + UX_Flow*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Copy + Code) / 17

Quick mode (4 specialists, 6 scored dimensions):
(Intent*3 + Originality*3 + UX_Flow*2 + Typography*2 + Color*2 + Layout) / 13

Read weights from ${CLAUDE_PLUGIN_ROOT}/config/scoring.json if available.
Scale: 1.0 to 4.0

Note: Specialist 6 (Intent/Originality/UX) returns THREE separate scores.
Each gets its own weight in the formula. Do not merge UX Flow into Intent.
</scoring_formula>

<verdict_rules>
The SHIP threshold depends on page type from Phase 1:

| Page Type | Threshold | Rationale |
|-----------|-----------|-----------|
| Admin / settings / docs | >= 2.5 | Template design is fine |
| Dashboard / form / e-commerce | >= 2.8 | Usability matters more than wow |
| Landing / marketing / SaaS | >= 3.0 | Creativity required |
| Portfolio / showcase | >= 3.5 | Design IS the product |
| Emotional / personal | >= 3.0 | Warmth and personality required |

Verdict logic:
- Score >= threshold AND no critical issues: SHIP
- Score within 0.3 of threshold AND fixable issues: CONDITIONAL SHIP
- Score < threshold - 0.3 OR critical issues: BLOCK
</verdict_rules>

<output_format>
Present the review using this structure:

## Design Review -- {page name}

**Verdict: SHIP / CONDITIONAL SHIP / BLOCK**
**Score: {weighted}/4.0**
**Page Type: {type} -- Creativity {required/appropriate/template-ok}**
**Mode: {Full (8/8) | Quick (4/8)} -- Tier {1|2|3}**

### Scores
| Specialist | Score | Weight | Key Finding |
|-----------|-------|--------|-------------|
{one row per scored dimension -- 10 rows in full mode, 6 in quick}

Show the calculation explicitly:
(I*3 + O*3 + UX*2 + T*2 + C*2 + L + Ic + M + Cp + Co) / 17 = N/17 = X.XX/4.0

The score in the header must equal the calculated weighted score exactly. Do not round or approximate differently.

### Cross-Specialist Findings (2+ agree -- highest confidence)
{merged issues with specialist sources}

### Top 5 Fixes (priority order)
1. {fix} -- found by {specialists}

### What Works (max 2 -- earned praise only)
{genuinely exceptional things, not filler}

### Gold-Standard Gap
"For a {page_type}, the best sites ({references}) would do X differently. The biggest gap is Y."
</output_format>
```

**Directive cleanup for boss:** The current Phase 3 has two "IMPORTANT" lines at lines 627 and 629. These are legitimate orchestrator instructions (clarifying the 3-score specialist and formula math), NOT aggressive directives. Convert them to normal-case clarifications inside the output_format section. Specifically:
- "IMPORTANT: Specialist 6 returns THREE scores..." becomes the note in scoring_formula: "Note: Specialist 6 (Intent/Originality/UX) returns THREE separate scores."
- "IMPORTANT: The Score in the header MUST equal..." becomes the instruction in output_format: "The score in the header must equal the calculated weighted score exactly."
  </action>
  <verify>
    <automated>
test -f skills/design-review/prompts/boss.md || { echo "MISSING: boss.md"; exit 1; }
grep -q '<role>' skills/design-review/prompts/boss.md || { echo "MISSING <role>"; exit 1; }
grep -q '<instructions>' skills/design-review/prompts/boss.md || { echo "MISSING <instructions>"; exit 1; }
grep -q '<scoring_formula>' skills/design-review/prompts/boss.md || { echo "MISSING <scoring_formula>"; exit 1; }
grep -q '<verdict_rules>' skills/design-review/prompts/boss.md || { echo "MISSING <verdict_rules>"; exit 1; }
grep -q '<output_format>' skills/design-review/prompts/boss.md || { echo "MISSING <output_format>"; exit 1; }
grep -q 'SHIP' skills/design-review/prompts/boss.md || { echo "MISSING verdict logic"; exit 1; }
grep -q '/ 17' skills/design-review/prompts/boss.md || { echo "MISSING full mode formula"; exit 1; }
grep -q '/ 13' skills/design-review/prompts/boss.md || { echo "MISSING quick mode formula"; exit 1; }
# No IMPORTANT in all-caps
ALLCAPS_IMPORTANT=$(grep -n '^.*\*\*IMPORTANT' skills/design-review/prompts/boss.md 2>/dev/null || true)
test -z "$ALLCAPS_IMPORTANT" || { echo "ALL-CAPS IMPORTANT found: $ALLCAPS_IMPORTANT"; exit 1; }
echo "boss.md has all required sections and no aggressive directives"
    </automated>
  </verify>
  <acceptance_criteria>
    - skills/design-review/prompts/boss.md exists
    - Contains XML tags: `<role>`, `<instructions>`, `<scoring_formula>`, `<verdict_rules>`, `<output_format>`
    - Contains full mode formula with "/ 17" and quick mode formula with "/ 13"
    - Contains all 5 page-type thresholds (Admin >= 2.5, Dashboard >= 2.8, Landing >= 3.0, Portfolio >= 3.5, Emotional >= 3.0)
    - Contains SHIP/CONDITIONAL SHIP/BLOCK verdict logic
    - Contains the output presentation template with Scores table, Cross-Specialist Findings, Top 5 Fixes, What Works, Gold-Standard Gap
    - Does NOT contain "**IMPORTANT:" (all-caps bold emphasis) -- uses normal-case instructions instead
    - The note about Specialist 6 returning THREE scores is present in scoring_formula section
  </acceptance_criteria>
  <done>Boss synthesizer prompt file created with scoring protocol, verdict rules, output format, and all orchestrator instructions converted from all-caps emphasis to normal-case text</done>
</task>

</tasks>

<verification>
All 9 prompt files exist in skills/design-review/prompts/ with correct XML structure:

```bash
# Count prompt files
PROMPT_COUNT=$(ls skills/design-review/prompts/*.md 2>/dev/null | wc -l | tr -d ' ')
test "$PROMPT_COUNT" -eq 9 || echo "Expected 9 prompt files, found $PROMPT_COUNT"

# Verify no aggressive directives in any prompt file
grep -rn 'FLAG SPECIFICALLY\|Find at least [0-9]\|\bNEVER\b' skills/design-review/prompts/ && echo "FAIL: aggressive directives found" || echo "PASS: no aggressive directives"

# Verify XML tags in all specialist files (not boss)
for f in font.md color.md layout.md icons.md motion.md intent.md copy.md code-a11y.md; do
  grep -q '<scoring_rubric>' "skills/design-review/prompts/$f" && echo "PASS: $f has scoring_rubric" || echo "FAIL: $f missing scoring_rubric"
done

# Verify boss has unique tags
grep -q '<scoring_formula>' skills/design-review/prompts/boss.md && echo "PASS: boss has scoring_formula" || echo "FAIL: boss missing scoring_formula"
grep -q '<verdict_rules>' skills/design-review/prompts/boss.md && echo "PASS: boss has verdict_rules" || echo "FAIL: boss missing verdict_rules"
```
</verification>

<success_criteria>
- 9 files exist in skills/design-review/prompts/ (font, color, layout, icons, motion, intent, copy, code-a11y, boss)
- All 8 specialist files have: role, instructions, scoring_rubric, output_format XML tags
- All 8 specialist files have 4-level rubrics with domain-specific anchors (not bare "Score 1-4")
- Boss file has: role, instructions, scoring_formula, verdict_rules, output_format XML tags
- Zero instances of "FLAG SPECIFICALLY", "Find at least N", or "NEVER" in any prompt file
- Gemini specialist prompts (color, layout) reference workspace copies (.color-reference.md, .layout-reference.md)
- Intent specialist prompt defines three separate scoring dimensions with separate rubric anchors
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-extraction-restructuring/08-01-SUMMARY.md`
</output>
