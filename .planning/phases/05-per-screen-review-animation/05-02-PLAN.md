---
phase: 05-per-screen-review-animation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - commands/design-audit.md
autonomous: true
requirements: [REVW-01, REVW-02, REVW-04, ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "After flow navigation completes, each captured screen receives a specialist review dispatched from design-audit.md"
    - "First and last screens receive full 8-specialist review, middle screens receive quick 4-specialist review"
    - "Animation detection JS is injected before each CTA click and collected after DOM stability"
    - "prefers-reduced-motion compliance is checked on each screen"
    - "Animation findings are stored in each screen's entry in flow-state.json"
    - "Per-screen scores are aggregated into an overall flow score with first/last screens weighted 1.5x"
    - "The flow summary box displays per-screen scores and overall flow score"
  artifacts:
    - path: "commands/design-audit.md"
      provides: "Per-screen review orchestration, animation detection, flow score aggregation"
      contains: "Per-Screen Review"
  key_links:
    - from: "commands/design-audit.md"
      to: "commands/design-review.md"
      via: "Specialist dispatch reuses design-review phases 1-3 per screen"
      pattern: "specialist|PAGE_BRIEF|boss.*synthesis"
    - from: "commands/design-audit.md"
      to: "config/flow-scoring.json"
      via: "Reads smart_weighting, flow_score, animation config at runtime"
      pattern: "smart_weighting|flow_score|animation"
    - from: "commands/design-audit.md"
      to: "skills/design-review/references/flow.md"
      via: "@reference already imported in command header"
      pattern: "Animation Detection|prefers-reduced-motion"
---

<objective>
Add per-screen specialist review dispatch, animation detection, and flow score aggregation to the design-audit command. After Phase 4's navigation captures screenshots, this new section reviews each screen using the existing 8-specialist system with smart weighting, detects CSS animations between screen transitions, and computes an overall flow score.

Purpose: This is the core value delivery of Phase 5 -- turning captured screenshots into scored, analyzed design feedback per screen, with animation quality assessment.
Output: Extended design-audit.md with new Sections 10-13 covering review dispatch, animation detection, flow score aggregation, and updated summary output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-per-screen-review-animation/05-CONTEXT.md
@.planning/phases/04-flow-navigation-engine/04-02-SUMMARY.md

@commands/design-audit.md
@commands/design-review.md
@config/flow-scoring.json
@config/scoring.json
@skills/design-review/references/flow.md
@shared/output.md

<interfaces>
<!-- Key contracts from Phase 4 that this plan consumes -->

From commands/design-audit.md (flow-state.json schema):
```json
{
  "version": "1.0",
  "status": "complete|dead_end|max_screens_reached|error",
  "flow_intent": "string or null",
  "url": "string",
  "mode": "intent|deterministic",
  "max_screens": 10,
  "screens": [
    {
      "number": 1,
      "name": "screen name",
      "slug": "screen-slug",
      "url": "https://...",
      "screenshot_path": "/tmp/design-audit-.../screen-1-slug.png",
      "timestamp": "ISO 8601",
      "cta_clicked": "CTA text or null",
      "cta_ref": "element ref or null"
    }
  ]
}
```

From config/scoring.json (specialist weights):
```json
{
  "weights": { "intent_match": 3, "originality": 3, "ux_flow": 2, "typography": 2, "color": 2, "layout": 1, "icons": 1, "motion": 1, "copy": 1, "code_a11y": 1 },
  "total_weight": 17,
  "quick_mode_weights": { "intent_match": 3, "originality": 3, "ux_flow": 2, "typography": 2, "color": 2, "layout": 1 },
  "quick_mode_total_weight": 13
}
```

From config/flow-scoring.json (new sections from Plan 01):
```json
{
  "smart_weighting": {
    "full_review_positions": ["first", "last"],
    "quick_review_positions": ["middle"],
    "quick_specialists": ["font", "color", "layout", "intent"]
  },
  "flow_score": {
    "position_weights": { "first": 1.5, "last": 1.5, "middle": 1.0 },
    "consistency_penalty_weight": 0.15
  },
  "animation": {
    "detection_method": "runtime",
    "inject_before_click": true,
    "inject_after_stable": true,
    "duration_ranges_ms": { ... },
    "easing_quality": { ... }
  }
}
```

From commands/design-review.md (specialist dispatch pattern):
- Phase 1: Haiku agent extracts PAGE_BRIEF (type, intent, audience, primary action, design bar)
- Phase 2: 8 specialists dispatched in parallel, each gets PAGE_BRIEF + screenshots + reference files
- Phase 3: Boss synthesizer merges findings, computes weighted score, produces verdict
- --quick mode: only specialists 1 (Font), 2 (Color), 3 (Layout), 6 (Intent/Originality/UX)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animation detection hooks to navigation loops</name>
  <files>commands/design-audit.md</files>
  <read_first>commands/design-audit.md, skills/design-review/references/flow.md, config/flow-scoring.json</read_first>
  <action>
Read the full design-audit.md (currently ~520 lines, Sections 1-9). Modify the **Intent Mode** loop (Section 6) and **Deterministic Mode** loop (Section 7) to inject animation detection at the right points. Also update the flow-state.json screen entry schema.

**Changes to Section 6 (Intent Mode):**

Between existing Step B (Reason about CTA) and Step C (Click the CTA), insert a new **Step B2: Inject animation listeners**:

```
### Step B2: Inject animation listeners

Before clicking, inject the animation event listener (from references/flow.md Section 9c) via `browser_evaluate` to capture transition/animation events triggered by the click:

[include the listener injection JS from flow.md Section 9c]

Also capture the pre-click animation state (from references/flow.md Section 9a) via `browser_evaluate`:

[include the pre-click state capture JS from flow.md Section 9a]

Store both results as `PRE_CLICK_ANIM_STATE` for this screen.
```

Between existing Step E (Font readiness check) and Step F (Confirm new screen), insert a new **Step E2: Capture post-transition animation state**:

```
### Step E2: Capture post-transition animation state

After DOM stability and font readiness, capture the post-stable animation state (references/flow.md Section 9b) via `browser_evaluate`:

[include the post-stable state capture JS from flow.md Section 9a -- same structure, captures current state]

Also collect the animation events that fired during the transition via `browser_evaluate`:
```javascript
(function() { return window.__spsk_anim_events || []; })()
```

Run the prefers-reduced-motion compliance check (references/flow.md Section 10) via `browser_evaluate`:

[include the PRM check JS from flow.md Section 10]

Store all results as `POST_STABLE_ANIM_STATE`, `ANIM_EVENTS`, and `PRM_CHECK` for this screen.
```

**Changes to Section 7 (Deterministic Mode):**

Add the same animation detection between Steps B and C (post-DOM-stability, pre-screenshot). In deterministic mode there is no CTA click, so only capture the current animation state and prefers-reduced-motion compliance -- no pre/post diff needed.

Insert after Step C (Font readiness check):
```
### Step C2: Capture animation state

Capture the current animation state (references/flow.md Section 9a) and prefers-reduced-motion compliance (references/flow.md Section 10) via `browser_evaluate`.

Store as `SCREEN_ANIM_STATE` and `PRM_CHECK`.
```

**Update Section 6 Step H and Section 7 Step E (flow-state.json screen entry):**

Add animation data to each screen entry:
```json
{
  "number": 1,
  "name": "...",
  "slug": "...",
  "url": "...",
  "screenshot_path": "...",
  "timestamp": "...",
  "cta_clicked": "...",
  "cta_ref": "...",
  "animation": {
    "pre_click": { "animations": [], "transitions": [] },
    "post_stable": { "animations": [], "transitions": [] },
    "events": [],
    "prefers_reduced_motion": {
      "has_support": false,
      "css_animation_count": 0,
      "active_animations": 0,
      "compliant": true,
      "verdict": "PASS"
    }
  }
}
```

For deterministic mode (no pre/post click), the `pre_click` field is null and `events` is empty -- only the current state and PRM check are captured.

**Key constraint:** Do NOT change the structure of existing steps (A through J in Section 6, A through F in Section 7). Insert new sub-steps (B2, E2, C2) between existing ones. All existing behavior must be preserved.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('commands/design-audit.md', 'utf8'); const checks = ['animation listeners', 'prefers-reduced-motion', 'PRE_CLICK_ANIM_STATE', 'POST_STABLE_ANIM_STATE', 'ANIM_EVENTS', '__spsk_anim_events', 'getAnimations'].every(s => content.includes(s)); console.log(checks ? 'PASS' : 'FAIL'); if(!checks) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Intent mode loop has Step B2 (pre-click animation injection) and Step E2 (post-stable animation capture + PRM check). Deterministic mode has Step C2 (animation state + PRM check). Flow-state.json screen entry schema includes animation object with pre_click, post_stable, events, and prefers_reduced_motion fields. All existing navigation steps unchanged.</acceptance_criteria>
  <done>Animation detection is injected at the correct points in both navigation modes. Each screen's flow-state.json entry includes animation data and prefers-reduced-motion compliance.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-screen review dispatch and flow score aggregation sections</name>
  <files>commands/design-audit.md</files>
  <read_first>commands/design-audit.md, commands/design-review.md, config/flow-scoring.json, config/scoring.json, shared/output.md</read_first>
  <action>
After Task 1's modifications, append new Sections 10-13 to design-audit.md. These sections run AFTER navigation completes (after existing Section 8 Flow Completion) but BEFORE the final summary.

**Restructure Section 8 (Flow Completion):**
- Move the branded summary display (existing 8b) and footer (existing 8d) to a new Section 13 (Final Summary). Section 8 should only update flow-state.json status (8a) and show a brief transition message:
```
✓ Navigation complete -- {N} screens captured. Starting per-screen review...
```

**New Section 10: Per-Screen Review Dispatch (per D-77, D-78)**

```markdown
## 10. Per-Screen Review Dispatch

After navigation completes with status "complete", "dead_end", or "max_screens_reached", dispatch specialist reviews for each captured screen.

**Skip review if** status is "error" and fewer than 2 screens were captured -- not enough data for meaningful review.

### 10a. Determine review mode per screen

Read `smart_weighting` from config/flow-scoring.json.

For each screen in flow-state.json `screens` array:
- If screen.number === 1 (first) OR screen.number === total_screens (last): **FULL mode** (8 specialists)
- Otherwise: **QUICK mode** (4 specialists: Font, Color, Layout, Intent)

Display the plan:
```
┌─ REVIEW PLAN ───────────────────────────────────────────────┐
│  Screen 1: {name} ── full (8 specialists)                    │
│  Screen 2: {name} ── quick (4 specialists)                   │
│  Screen 3: {name} ── quick (4 specialists)                   │
│  Screen 4: {name} ── full (8 specialists)                    │
└──────────────────────────────────────────────────────────────┘
```

**Edge case:** If only 1 screen captured, it is both first AND last -- run full mode. If only 2 screens captured, both are first/last -- both get full mode.

### 10b. Dispatch per-screen reviews

For each screen, follow the design-review.md workflow adapted for flow context:

**Phase 1 (Page Analysis):** Instead of running the Haiku agent fresh, provide the flow-level PAGE_BRIEF:
- Page TYPE: derive from the screen's content (the flow intent provides context)
- PAGE INTENT: "{flow_intent} -- Screen {N}: {screen_name}"
- The flow intent enriches per-screen intent -- Screen 1's intent is "entry point for {flow}", last screen's is "completion of {flow}"

**Phase 2 (Specialist Dispatch):** Run specialists per design-review.md Phase 2.
- Full mode: all 8 specialists
- Quick mode: specialists 1 (Font), 2 (Color), 3 (Layout), 6 (Intent/Originality/UX)
- Each specialist receives: the screen's screenshot (from screenshot_path), source code access via the browser, and the PAGE_BRIEF with flow context
- **Animation enrichment (per ANIM-03):** For the Motion specialist (#5, full mode only), append the screen's animation data from flow-state.json to the prompt:
  ```
  ANIMATION DATA FROM FLOW NAVIGATION:
  - Pre-click state: {animation.pre_click}
  - Post-stable state: {animation.post_stable}
  - Events during transition: {animation.events}
  - prefers-reduced-motion: {animation.prefers_reduced_motion.verdict}

  Evaluate animation QUALITY using this runtime data alongside your source code analysis.
  Flag any animations that lack prefers-reduced-motion support.
  Assess transition timing against the ranges in config/flow-scoring.json.
  ```

**Phase 3 (Boss Synthesis):** Run the boss synthesizer per design-review.md Phase 3.
- Use weights from config/scoring.json (full mode: /17, quick mode: /13)
- Produce per-screen score, findings, and verdict

### 10c. Store per-screen results

After each screen's review completes, update that screen's entry in flow-state.json:
```json
{
  "number": 1,
  "name": "...",
  "review": {
    "mode": "full|quick",
    "specialist_count": 8,
    "scores": {
      "intent_match": 3.0,
      "originality": 2.5,
      "ux_flow": 3.0,
      "typography": 3.5,
      "color": 2.0,
      "layout": 3.0,
      "icons": 2.5,
      "motion": 3.0,
      "copy": 3.0,
      "code_a11y": 2.5
    },
    "weighted_score": 2.85,
    "verdict": "CONDITIONAL",
    "findings": [...],
    "cross_specialist_findings": [...]
  }
}
```
For quick mode screens, only the 4 specialist scores (mapped to the 6 scored dimensions from design-review.md quick mode) are present. Missing dimensions are null.

Write flow-state.json after EACH screen review completes (progressive persistence).

### 10d. Display per-screen progress

After each screen review, show branded output:
```
✓ Screen {N}: {name} ── {score}/10 ({verdict}) ── {mode} mode
```

(Score converted to /10 display scale per shared/output.md: internal * 2.5)
```

**New Section 11: Animation Summary (per ANIM-01, ANIM-02, ANIM-03)**

```markdown
## 11. Animation Summary

After all per-screen reviews complete, summarize animation findings across the flow.

### 11a. Compile animation data

Read the `animation` field from each screen in flow-state.json.

### 11b. Animation quality assessment

For each screen, evaluate:
1. **Transition coverage:** Did interactive elements (buttons, links, form fields) have CSS transitions? List elements with and without.
2. **Duration quality:** Compare detected durations against config/flow-scoring.json `animation.duration_ranges_ms`. Flag durations outside expected ranges.
3. **Easing quality:** Compare detected timing functions against config `animation.easing_quality`. Flag `linear` easing on UI elements.
4. **Cross-screen transition events:** How many animation/transition events fired during each screen transition? Zero events on a navigation = missing page transitions (flag as finding).

### 11c. prefers-reduced-motion summary

Compile PRM results across all screens:
- How many screens passed PRM check?
- Which screens have animations without PRM support?
- Overall PRM compliance: PASS (all screens) / PARTIAL (some) / FAIL (none)

### 11d. Store animation summary

Add a top-level `animation_summary` to flow-state.json:
```json
{
  "animation_summary": {
    "total_transitions_detected": 12,
    "total_animation_events": 8,
    "screens_with_animations": [1, 3, 4],
    "screens_without_transitions": [2],
    "prefers_reduced_motion": {
      "compliant_screens": 3,
      "non_compliant_screens": 1,
      "overall": "PARTIAL"
    },
    "findings": [
      "Screen 2 has no transition events -- navigation feels abrupt",
      "Screen 3: linear easing on button hover (use ease-in-out)",
      "Screen 4: animations lack prefers-reduced-motion support"
    ]
  }
}
```
```

**New Section 12: Flow Score Aggregation (per D-80, REVW-04)**

```markdown
## 12. Flow Score Aggregation

### 12a. Compute weighted flow score

Read `flow_score` config from flow-scoring.json.

For each screen:
- Get the screen's weighted_score (from review.weighted_score)
- Get the position weight: first=1.5, last=1.5, middle=1.0

Formula:
```
flow_score = sum(screen_score * position_weight) / sum(position_weight)
```

Example with 4 screens:
```
Screen 1 (first): 3.0 * 1.5 = 4.5
Screen 2 (middle): 2.5 * 1.0 = 2.5
Screen 3 (middle): 2.8 * 1.0 = 2.8
Screen 4 (last): 3.2 * 1.5 = 4.8
Total weighted = 14.6, total weights = 5.0
flow_score = 14.6 / 5.0 = 2.92
```

**Note:** Consistency penalty (from Plan 03) will further adjust this score. At this stage, compute the pre-penalty flow score.

### 12b. Determine flow verdict

Use the page type from the first screen's PAGE_BRIEF and the thresholds from config/scoring.json:
- SHIP: flow_score >= threshold AND no critical cross-screen issues
- CONDITIONAL: flow_score within 0.3 of threshold AND issues are fixable
- BLOCK: flow_score < threshold - 0.3 OR critical issues found

### 12c. Store flow score

Add to flow-state.json top level:
```json
{
  "flow_score": {
    "weighted_score": 2.92,
    "display_score": 7.3,
    "verdict": "CONDITIONAL",
    "position_weights_applied": { "1": 1.5, "2": 1.0, "3": 1.0, "4": 1.5 },
    "page_type": "landing",
    "threshold": 3.0,
    "consistency_penalty": null
  }
}
```
(consistency_penalty is null until Plan 03's consistency pass fills it in)
```

**New Section 13: Flow Review Summary (moved + extended from old 8b)**

```markdown
## 13. Flow Review Summary

Display the complete flow audit results using branded output.

### 13a. Per-screen score table

```
┌─ FLOW REVIEW ───────────────────────────────────────────────┐
│                                                               │
│  Screen 1: {name}                                             │
│  {score_bar} {display_score}/10  ·  {verdict}  ·  full        │
│  Top finding: {highest priority finding}                      │
│                                                               │
│  Screen 2: {name}                                             │
│  {score_bar} {display_score}/10  ·  {verdict}  ·  quick       │
│  Top finding: {highest priority finding}                      │
│  ...                                                          │
└──────────────────────────────────────────────────────────────┘
```

### 13b. Animation findings box

```
┌─ ANIMATION ─────────────────────────────────────────────────┐
│  Transitions: {total} detected across {N} screens            │
│  Events: {total} animation/transition events captured         │
│  prefers-reduced-motion: {PASS|PARTIAL|FAIL}                 │
│                                                               │
│  {finding 1}                                                  │
│  {finding 2}                                                  │
└──────────────────────────────────────────────────────────────┘
```

### 13c. Flow verdict box

```
┌─ FLOW VERDICT ──────────────────────────────────────────────┐
│  {score_bar} {display_score}/10  ·  {verdict}                │
│                                                               │
│  {N} screens  ·  {full_count} full  ·  {quick_count} quick   │
│  Weakest: Screen {N} ({name}) at {score}/10                  │
│  Strongest: Screen {N} ({name}) at {score}/10                │
└──────────────────────────────────────────────────────────────┘
```

### 13d. Top 5 flow-wide fixes

Aggregate findings across all screens, deduplicate, and rank by:
1. Cross-specialist agreement (found by 2+ specialists across screens)
2. Severity (critical > high > medium)
3. Frequency (appears on multiple screens)

```
### Top 5 Fixes
1. {fix} -- Screen(s) {N, M} -- {specialist(s)}
2. ...
```

### 13e. Next step hint

```
Next: Run Phase 6 to generate the HTML diagnostic report
```

### 13f. Footer

```
github.com/spsk-dev/tasteful-design
```
```

**Update the `allowed-tools` frontmatter** at the top of design-audit.md to add Gemini access (needed for Color and Layout specialists in per-screen review):
```
allowed-tools: Bash(mkdir *), Bash(cp *), Bash(rm *), Bash(cat *), Bash(npx *), Bash(gemini *), Bash(which *), Bash(kill *), Bash(lsof *)
```

**Update the Key Constraints section (Section 9):**
- Remove the constraint "Phase 4 is navigation + capture only. Do NOT import or dispatch specialist reviews -- that is Phase 5." since we are now IN Phase 5.
- Add: "Sections 10-13 run only after navigation completes. If navigation errors with <2 screens, skip review and show error summary."
- Add: "Per-screen reviews are sequential (not parallel) to manage token budget. Each screen review completes fully before the next starts."
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('commands/design-audit.md', 'utf8'); const checks = ['Per-Screen Review Dispatch', 'Animation Summary', 'Flow Score Aggregation', 'Flow Review Summary', 'smart_weighting', 'flow_score', 'weighted_score', 'FLOW VERDICT', 'FLOW REVIEW', 'ANIMATION'].every(s => content.includes(s)); console.log(checks ? 'PASS' : 'FAIL'); if(!checks) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>design-audit.md has Sections 10 (per-screen review dispatch with full/quick mode), 11 (animation summary), 12 (flow score aggregation), and 13 (branded summary output). Section 10 dispatches specialists per design-review.md pattern with flow context enrichment. Motion specialist receives animation data from navigation. Flow score uses weighted average with 1.5x for first/last. Key Constraints updated to allow review dispatch. allowed-tools includes gemini.</acceptance_criteria>
  <done>design-audit.md is a complete flow audit command: navigate screens (Phase 4) then review each screen with specialists, detect animations, aggregate into flow score, and display branded results.</done>
</task>

</tasks>

<verification>
1. `wc -l commands/design-audit.md` -- should be significantly longer than the original ~520 lines (expect ~900-1100 lines)
2. `grep -c "Section\|## [0-9]" commands/design-audit.md` -- should show 13+ sections
3. `grep "Per-Screen Review\|Animation Summary\|Flow Score\|Flow Review Summary" commands/design-audit.md` -- all 4 new sections present
4. `grep "smart_weighting\|flow_score.*config\|animation\.duration" commands/design-audit.md` -- references Plan 01's config
5. `grep "design-review.md\|PAGE_BRIEF\|specialist" commands/design-audit.md` -- references the existing review system
6. `grep "prefers-reduced-motion\|ANIM_EVENTS\|getAnimations" commands/design-audit.md` -- animation detection present
</verification>

<success_criteria>
- design-audit.md contains Sections 1-13 covering navigation (1-9), review (10), animation (11), scoring (12), and summary (13)
- First/last screens get 8-specialist review, middle screens get 4-specialist review
- Animation detection hooks are in both intent and deterministic navigation loops
- Motion specialist receives animation runtime data alongside source analysis
- PRM compliance checked on every screen
- Flow score is a weighted average with position-based weights
- Branded output shows per-screen scores, animation findings, and overall flow verdict
</success_criteria>

<output>
After completion, create `.planning/phases/05-per-screen-review-animation/05-02-SUMMARY.md`
</output>
