---
phase: 05-per-screen-review-animation
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - commands/design-audit.md
autonomous: true
requirements: [REVW-03]

must_haves:
  truths:
    - "After all per-screen reviews complete, a consistency analysis pass compares visual properties across screens"
    - "Button style drift between screens is detected and flagged"
    - "Color palette inconsistencies across screens are detected and flagged"
    - "Spacing/padding drift between screens is detected and flagged"
    - "Typography mismatches across screens are detected and flagged"
    - "Consistency findings are stored in flow-state.json as a top-level consistency section"
    - "Consistency findings apply a penalty to the overall flow score"
  artifacts:
    - path: "commands/design-audit.md"
      provides: "Cross-screen consistency analysis pass"
      contains: "Cross-Screen Consistency"
  key_links:
    - from: "commands/design-audit.md (Section 12.5)"
      to: "flow-state.json screens[].review.findings"
      via: "Reads per-screen specialist findings to compare across screens"
      pattern: "consistency|drift|mismatch"
    - from: "commands/design-audit.md (Section 12.5)"
      to: "config/flow-scoring.json consistency"
      via: "Reads consistency thresholds and severity levels"
      pattern: "consistency_thresholds|color_drift_delta_e|spacing_tolerance"
---

<objective>
Add cross-screen consistency analysis to the design-audit command. This is a post-processing pass that runs after all per-screen reviews complete (after Section 11) but before flow score aggregation (Section 12). It compares visual properties across screens and flags drift in buttons, colors, spacing, typography, and component variants.

Purpose: Cross-screen consistency is the killer differentiator (per FEATURES.md research). A per-page review misses when the same button is styled differently on screen 3 vs screen 1 -- this pass catches that.
Output: New Section 12 (renumbering existing 12->13, 13->14) in design-audit.md with consistency analysis logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-per-screen-review-animation/05-CONTEXT.md
@.planning/phases/05-per-screen-review-animation/05-02-PLAN.md

@commands/design-audit.md
@config/flow-scoring.json
@skills/design-review/references/flow.md
@shared/output.md

<interfaces>
<!-- Per-screen review data that consistency analysis reads (from Plan 02) -->

From flow-state.json after Plan 02 (per-screen review output):
```json
{
  "screens": [
    {
      "number": 1,
      "name": "Welcome",
      "review": {
        "mode": "full",
        "scores": { "typography": 3.5, "color": 2.0, "layout": 3.0, ... },
        "findings": [
          { "specialist": "font", "severity": "medium", "text": "Body text 16px, heading 48px Inter" },
          { "specialist": "color", "severity": "low", "text": "Primary blue #3B82F6, accent #10B981" },
          { "specialist": "layout", "severity": "medium", "text": "Section padding 64px, card gap 24px" }
        ]
      }
    },
    {
      "number": 2,
      "name": "Details",
      "review": {
        "mode": "quick",
        "scores": { "typography": 3.0, "color": 2.5, "layout": 2.5, ... },
        "findings": [
          { "specialist": "font", "severity": "medium", "text": "Body text 14px, heading 36px Poppins" },
          { "specialist": "color", "severity": "low", "text": "Primary blue #2563EB, accent #059669" },
          { "specialist": "layout", "severity": "medium", "text": "Section padding 32px, card gap 16px" }
        ]
      }
    }
  ]
}
```

From config/flow-scoring.json (consistency config from Plan 01):
```json
{
  "consistency": {
    "checks": ["button_style", "color_palette", "spacing", "typography", "component_variants"],
    "severity_levels": { "drift": "warning", "mismatch": "issue", "conflict": "critical" },
    "thresholds": { "color_drift_delta_e": 10, "spacing_tolerance_px": 4, "font_size_tolerance_px": 2 }
  },
  "flow_score": {
    "consistency_penalty_weight": 0.15
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-screen consistency analysis section to design-audit.md</name>
  <files>commands/design-audit.md</files>
  <read_first>commands/design-audit.md, config/flow-scoring.json, skills/design-review/references/flow.md</read_first>
  <action>
Read the current design-audit.md (which now has Sections 1-13 from Plan 02). Insert a new **Section 12: Cross-Screen Consistency Analysis** between the current Section 11 (Animation Summary) and Section 12 (Flow Score Aggregation). Renumber the existing Section 12 to 13 and Section 13 to 14.

The new Section 12 implements decisions D-81 (post-review comparison pass), D-82 (what to flag), D-83 (results in flow-state.json), and D-84 (not a 9th specialist -- a post-processing pass).

```markdown
## 12. Cross-Screen Consistency Analysis

**Runs after all per-screen reviews complete (Section 10) and animation summary (Section 11).**

This is a POST-PROCESSING pass (per D-84), not a 9th specialist. It reads the per-screen specialist findings from flow-state.json and compares visual properties across all screens.

**Skip if** fewer than 2 screens have reviews (nothing to compare).

### 12a. Extract comparable properties from specialist findings

For each screen, extract design properties from the specialist findings and scores:

**From Font specialist (#1) findings:**
- Body text font family and size
- Heading font family, size, and weight
- Line-height values

**From Color specialist (#2) findings:**
- Primary color (most dominant non-white/non-black)
- Secondary/accent colors
- Background colors
- Button colors

**From Layout specialist (#3) findings:**
- Section padding values
- Card/component gap values
- Content max-width
- Grid column counts

**From specialist findings text (pattern matching):**
- Button border-radius, background-color, padding, font-weight mentions
- Component descriptions (card style, nav pattern, input style)

If a finding mentions a specific CSS value (e.g., "16px body text", "#3B82F6 primary", "24px gap"), extract it as a comparable property.

### 12b. Compare across screens

Read `consistency.thresholds` from config/flow-scoring.json.

For each consistency check in `consistency.checks`:

**Button style drift:**
Compare button-related properties across screens. Flag when:
- Border-radius differs between screens (e.g., rounded-lg on screen 1, rounded on screen 3)
- Button background colors differ for primary CTAs
- Button padding or font-weight differs

**Color palette consistency:**
Compare extracted colors across screens. Flag when:
- Primary color varies between screens (Delta E > `color_drift_delta_e` threshold)
- New accent colors appear on some screens but not others
- Background color changes unexpectedly between screens

**Spacing/padding patterns:**
Compare spacing values. Flag when:
- Section padding differs by more than `spacing_tolerance_px` between screens
- Card/component gaps are inconsistent
- Content max-width changes between screens

**Typography consistency:**
Compare font properties. Flag when:
- Body text size differs by more than `font_size_tolerance_px` between screens
- Font family changes between screens (e.g., Inter on screen 1, Poppins on screen 3)
- Heading hierarchy breaks (h1 size on screen 2 < h1 size on screen 1)

**Component variant drift:**
Compare how repeated components are described across screens. Flag when:
- Cards have different styling on different screens
- Navigation looks different between screens
- Form inputs have different styling

### 12c. Classify severity

For each finding, assign severity from `consistency.severity_levels`:

- **drift (warning):** Subtle variation that might be intentional (e.g., 2px spacing difference)
- **mismatch (issue):** Clear inconsistency that looks unintentional (e.g., different font family on one screen)
- **conflict (critical):** Contradictory styles that break visual coherence (e.g., rounded buttons on 3 screens, square on 1)

### 12d. Store consistency results

Add a top-level `consistency` section to flow-state.json (per D-83):
```json
{
  "consistency": {
    "findings": [
      {
        "check": "typography",
        "severity": "mismatch",
        "description": "Body text is 16px on Screens 1, 3, 4 but 14px on Screen 2",
        "screens_affected": [2],
        "screens_reference": [1, 3, 4]
      },
      {
        "check": "color_palette",
        "severity": "drift",
        "description": "Primary blue shifts from #3B82F6 (Screen 1) to #2563EB (Screen 2-4)",
        "screens_affected": [2, 3, 4],
        "screens_reference": [1]
      },
      {
        "check": "spacing",
        "severity": "conflict",
        "description": "Section padding is 64px on Screens 1, 4 but 32px on Screens 2, 3",
        "screens_affected": [2, 3],
        "screens_reference": [1, 4]
      }
    ],
    "summary": {
      "total_findings": 3,
      "critical": 1,
      "issues": 1,
      "warnings": 1
    }
  }
}
```

### 12e. Apply consistency penalty to flow score

Read `flow_score.consistency_penalty_weight` from config (0.15).

Compute penalty:
```
critical_count = number of "conflict" severity findings
issue_count = number of "mismatch" severity findings
warning_count = number of "drift" severity findings

penalty_ratio = min(1.0, (critical_count * 0.5 + issue_count * 0.2 + warning_count * 0.05))
consistency_penalty = penalty_ratio * consistency_penalty_weight
```

The penalty is subtracted from the flow score in Section 13 (Flow Score Aggregation):
```
final_flow_score = weighted_flow_score - (weighted_flow_score * consistency_penalty)
```

Update `flow_score.consistency_penalty` in flow-state.json with the computed penalty value.

### 12f. Display consistency findings

```
┌─ CONSISTENCY ───────────────────────────────────────────────┐
│  {total} findings: {critical} critical · {issues} issues · {warnings} warnings
│                                                               │
│  ✗ {critical finding 1}                                       │
│  ⚠ {issue finding 1}                                         │
│  ○ {warning finding 1}                                        │
│                                                               │
│  Score penalty: -{penalty_percentage}%                        │
└──────────────────────────────────────────────────────────────┘
```

If no consistency findings: show `✓ No cross-screen consistency issues detected` and skip the penalty.
```

**Update Section 13 (formerly Section 12 -- Flow Score Aggregation):**

Modify the flow score computation to apply the consistency penalty:
- After computing the weighted average, subtract the consistency penalty:
  ```
  final_flow_score = weighted_flow_score * (1 - consistency_penalty)
  ```
- Update the flow_score object in flow-state.json to include `consistency_penalty` (no longer null)
- Update the display score accordingly

**Update Section 14 (formerly Section 13 -- Flow Review Summary):**

Add the consistency findings box between the animation box and the flow verdict box in the summary output. The consistency findings should appear in the branded output after animation and before the final verdict.

Also update the "Top 5 flow-wide fixes" to include consistency findings alongside per-screen findings. Consistency issues that are "conflict" severity should rank higher than single-screen issues.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('commands/design-audit.md', 'utf8'); const checks = ['Cross-Screen Consistency', 'consistency_penalty', 'button_style', 'color_palette', 'spacing', 'typography', 'component_variants', 'CONSISTENCY', 'severity', 'drift', 'mismatch', 'conflict'].every(s => content.includes(s)); console.log(checks ? 'PASS' : 'FAIL'); if(!checks) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>design-audit.md has a Cross-Screen Consistency Analysis section that: (1) extracts comparable properties from per-screen specialist findings, (2) compares them across screens using thresholds from config, (3) classifies findings as drift/mismatch/conflict severity, (4) stores results in flow-state.json consistency section, (5) computes a score penalty, (6) displays findings in branded output box. Flow Score Aggregation section applies the consistency penalty. Summary includes consistency box. Sections are numbered correctly (12 = consistency, 13 = flow score, 14 = summary).</acceptance_criteria>
  <done>Cross-screen consistency analysis detects visual drift between screens in a flow, flags button/color/spacing/typography/component inconsistencies, applies a score penalty, and displays findings in the branded flow summary.</done>
</task>

<task type="auto">
  <name>Task 2: Update flow.md reference hints and design-audit key constraints</name>
  <files>commands/design-audit.md</files>
  <read_first>commands/design-audit.md</read_first>
  <action>
After Task 1, make two final cleanup edits to design-audit.md:

**1. Update the @reference import at the top of the file:**

The file header currently imports:
```
@${CLAUDE_PLUGIN_ROOT}/shared/output.md
@${CLAUDE_PLUGIN_ROOT}/skills/design-review/references/flow.md
```

Verify both imports are still present. No changes needed -- flow.md already contains the animation and consistency sections from Plan 01.

**2. Update Key Constraints section:**

Ensure the Key Constraints section (near the end of the file) includes these rules:

- "Per-screen reviews run sequentially, not in parallel. Each screen's review must complete before the next starts. This manages token budget (Pitfall 10 from PITFALLS.md)."
- "Consistency analysis runs AFTER all per-screen reviews. It is a post-processing pass, not a 9th specialist (per D-84)."
- "Animation detection hooks must NOT disrupt the navigation loop. If browser_evaluate fails for animation injection, log the error and proceed without animation data for that screen."
- "The flow-state.json is the contract between navigation (Sections 1-9), review (Sections 10-11), consistency (Section 12), scoring (Section 13), and reporting (Phase 6)."
- "Progressive persistence: write flow-state.json after each screen review, after consistency analysis, and after flow score computation."

**3. Add a section count comment at the very end of the file:**

```
<!-- design-audit.md: 14 sections — navigation (1-9), review (10), animation (11), consistency (12), scoring (13), summary (14) -->
```

This helps future editors understand the file structure at a glance.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('commands/design-audit.md', 'utf8'); const checks = ['sequentially', 'post-processing pass', 'animation detection hooks', 'Progressive persistence', '14 sections'].every(s => content.includes(s)); console.log(checks ? 'PASS' : 'FAIL'); if(!checks) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Key Constraints section lists sequential review, post-processing consistency, animation error tolerance, flow-state.json as contract, and progressive persistence rules. File ends with a section count comment. @reference imports are present at top.</acceptance_criteria>
  <done>design-audit.md has complete and accurate key constraints reflecting the full Phase 5 implementation, with a section count comment for maintainability.</done>
</task>

</tasks>

<verification>
1. `grep -c "## [0-9]" commands/design-audit.md` -- should show 14+ section headers
2. `grep "Cross-Screen Consistency\|consistency_penalty\|CONSISTENCY" commands/design-audit.md` -- consistency analysis present
3. `grep "button_style\|color_palette\|spacing\|typography\|component_variants" commands/design-audit.md` -- all 5 consistency checks present
4. `grep "14 sections" commands/design-audit.md` -- section count comment present
5. `grep "sequentially\|post-processing\|Progressive persistence" commands/design-audit.md` -- key constraints updated
</verification>

<success_criteria>
- design-audit.md has 14 sections covering the full flow audit lifecycle
- Cross-screen consistency analysis compares 5 dimensions (button, color, spacing, typography, components)
- Findings classified as drift/mismatch/conflict with score penalty
- Consistency results stored in flow-state.json top-level consistency section
- Flow score penalized by consistency issues
- Branded output includes consistency findings box
- Key constraints reflect the full Phase 5 implementation
</success_criteria>

<output>
After completion, create `.planning/phases/05-per-screen-review-animation/05-03-SUMMARY.md`
</output>
