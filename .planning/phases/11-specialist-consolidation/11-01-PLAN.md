---
phase: 11-specialist-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design-review/prompts/intent.md
  - skills/design-review/prompts/copy.md
  - config/scoring.json
  - skills/design-review/prompts/boss.md
  - commands/design-review.md
autonomous: true
requirements: [SPEC-01, SPEC-02]

must_haves:
  truths:
    - "Intent specialist prompt contains copy quality evaluation instructions and 4th sub-score"
    - "scoring.json total_weight equals 16 and copy key is replaced by copy_quality"
    - "Boss formula divides by 16 with 9 scored dimensions instead of 10"
    - "design-review.md dispatches 7 specialists with no Copy specialist section"
    - "copy.md prompt file no longer exists"
  artifacts:
    - path: "skills/design-review/prompts/intent.md"
      provides: "Merged Intent/Originality/UX/Copy specialist with 4 sub-scores"
      contains: "copy_quality"
    - path: "config/scoring.json"
      provides: "Updated weights with copy_quality replacing copy"
      contains: "total_weight.*16"
    - path: "skills/design-review/prompts/boss.md"
      provides: "Updated scoring formula for 7 specialists"
      contains: "CpQ"
    - path: "commands/design-review.md"
      provides: "7-specialist dispatch with no Copy section"
  key_links:
    - from: "skills/design-review/prompts/intent.md"
      to: "config/scoring.json"
      via: "copy_quality key exists in both"
      pattern: "copy_quality"
    - from: "skills/design-review/prompts/boss.md"
      to: "config/scoring.json"
      via: "formula divisor matches total_weight"
      pattern: "/ 16"
---

<objective>
Merge the Copy specialist into Intent/Originality/UX and make the core system functional as 7 specialists.

Purpose: This is the atomic core change -- intent.md absorbs copy evaluation as a 4th sub-score, scoring weights update from /17 to /16, the boss formula reflects 9 scored dimensions, and design-review.md dispatches 7 specialists instead of 8.

Output: A functionally correct 7-specialist system where `/design-review` runs without the standalone Copy specialist.
</objective>

<execution_context>
@/Users/felipemachado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/felipemachado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-specialist-consolidation/11-CONTEXT.md

Key interfaces the executor needs:

Current intent.md output schema (3 sub-scores):
```json
{
  "specialist": "intent",
  "scores": {
    "intent_match": 3,
    "originality": 2,
    "ux_flow": 3
  },
  "findings": [...],
  "summary": "..."
}
```

Target intent.md output schema (4 sub-scores):
```json
{
  "specialist": "intent",
  "scores": {
    "intent_match": 3,
    "originality": 2,
    "ux_flow": 3,
    "copy_quality": 3
  },
  "findings": [...],
  "summary": "..."
}
```

Current scoring.json weights:
```json
{
  "intent_match": 3, "originality": 3, "ux_flow": 2,
  "typography": 2, "color": 2, "layout": 1,
  "icons": 1, "motion": 1, "copy": 1, "code_a11y": 1,
  "total_weight": 17
}
```

Current boss.md formula:
```
(Intent*3 + Originality*3 + UX_Flow*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Copy + Code) / 17
```

Current design-review.md Specialist 7 (lines ~377-381):
```
### Specialist 7: Copy & Language (Claude Haiku Agent)
@${CLAUDE_PLUGIN_ROOT}/skills/design-review/prompts/copy.md
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Copy into Intent prompt and update scoring.json</name>
  <read_first>
    - skills/design-review/prompts/intent.md (merge target -- current 3 sub-scores)
    - skills/design-review/prompts/copy.md (merge source -- content to absorb then delete)
    - config/scoring.json (weights to update atomically)
  </read_first>
  <files>
    skills/design-review/prompts/intent.md,
    config/scoring.json,
    skills/design-review/prompts/copy.md
  </files>
  <action>
  **A. Merge copy.md content into intent.md (per SPEC-01):**

  1. Update the `<role>` tag -- add "copy quality assessment" to the expertise list. The first sentence becomes:
     "You are a design intent, originality, UX, and copy quality specialist evaluating frontend design quality."
     Add "copy quality, tone matching, CTA language, terminology consistency" to the expertise list.

  2. Update the `<instructions>` tag:
     - Change "Evaluate design intent, originality, and UX flow for this page across three dimensions" to "four dimensions"
     - Change "Identify 3-5 issues across your three dimensions" to "Identify 3-6 issues across your four dimensions (cover each dimension)"
     - Add "Dimension 4 -- Copy Quality:" block AFTER the UX Flow dimension. Content adapted from copy.md's review checklist:
       ```
       Dimension 4 -- Copy Quality:
       Does the page copy serve the design intent?
       - Spelling and grammar: any errors, missing accents/diacritics (especially Spanish)?
       - Placeholder detection: lorem ipsum, "Your text here", TODO still present?
       - Generic labels: "Submit", "OK", "Click Here", "Learn More" without context?
       - Tone match: does the copy tone fit the page type and audience?
       - CTA quality: are calls-to-action specific and compelling, or vague?
       - Consistency: same concept described differently in different places?
       ```
     - Update the "Check for and flag" list to include copy items:
       "AI slop patterns, designs that fight their content's intent, broken user flows, buried CTAs, unclear next steps, missing states, placeholder text, generic labels, tone mismatches"

  3. Update the `<scoring_rubric>` tag:
     - Change "Score each of the three dimensions" to "Score each of the four dimensions"
     - Add Copy Quality rubric AFTER UX Flow rubric, adapted from copy.md:
       ```
       Copy Quality:
       - 1 (Poor): Placeholder text (lorem ipsum), spelling errors, missing diacritics, generic labels ("Submit", "Click Here"), tone mismatch with page purpose.
       - 2 (Below Average): No placeholders but generic copy ("Build the future"), some inconsistent terminology, CTAs not compelling.
       - 3 (Good): Clear, correct copy with appropriate tone, specific CTAs, minor issues (one generic phrase, slight inconsistency).
       - 4 (Excellent): Specific, compelling copy matching page voice, perfect grammar/diacritics, consistent terminology, CTAs that drive action.
       ```

  4. Update the `<output_format>` tag:
     - In the JSON example, add `"copy_quality": 3` to the scores object (4 keys now)
     - In the Requirements list, change "scores: object with intent_match, originality, ux_flow (each integer 1-4)" to "scores: object with intent_match, originality, ux_flow, copy_quality (each integer 1-4)"
     - Add a finding example with `"dimension": "copy_quality"` to show the expected format

  **B. Update scoring.json (per SPEC-02):**

  1. In `weights`: remove the `"copy": 1` entry, add `"copy_quality": 1` entry. Keep all other weights identical.
  2. Change `"total_weight": 17` to `"total_weight": 16`. Rationale: 3+3+2+2+2+1+1+1+1+1 = 17 minus copy(1) = 16. The copy_quality weight of 1 is part of the Intent specialist's contribution but replaces the standalone Copy weight, so net effect is 17-1=16.
     Wait -- let me verify: intent_match(3) + originality(3) + ux_flow(2) + typography(2) + color(2) + layout(1) + icons(1) + motion(1) + copy_quality(1) + code_a11y(1) = 17. That is still 17 with copy_quality replacing copy.
     Actually re-reading the CONTEXT.md decision: "total_weight goes from 17 to 16 because Copy's standalone weight (1) is removed." But if copy_quality gets weight 1, it replaces copy's weight 1, so the sum is still 17.
     The CONTEXT.md says the net is 17-1=16 but that math only works if copy_quality does NOT get its own weight entry in the weights object. Re-reading more carefully: "intent weight stays 3, copy_quality gets own weight of 1, total_weight: 17->16 (net: removed Copy standalone, added copy_quality to Intent)." This is contradictory -- if copy_quality gets weight 1, sum is still 17.
     Resolution: Follow the user's stated intent of total_weight=16. This means copy_quality sub-score does NOT get a separate weight entry in the weights object. Instead, copy_quality is folded into the Intent specialist's existing weight allocation. The Intent specialist produces 4 scores but the boss averages them into 3 weighted contributions (intent_match*3, originality*3, ux_flow*2) plus copy_quality is unweighted or implicitly part of the intent assessment.
     ACTUALLY -- simplest interpretation matching both "total_weight: 16" and "7 specialists": remove the `"copy": 1` line entirely. Do NOT add `"copy_quality"` as a separate weighted dimension. The Intent specialist produces copy_quality as informational output but it does not factor into the weighted score. 10 dimensions -> 9 dimensions. Sum: 3+3+2+2+2+1+1+1+1 = 16. This matches.
  3. `quick_mode_weights` and `quick_mode_total_weight`: Copy was NOT in quick mode. Quick mode weights stay exactly the same: intent_match(3)+originality(3)+ux_flow(2)+typography(2)+color(2)+layout(1) = 13. No change needed.

  **C. Delete copy.md:**
  Delete the file `skills/design-review/prompts/copy.md` after confirming the merge is complete.
  </action>
  <verify>
    <automated>cd /Users/felipemachado/Sites/spsk && grep -q "copy_quality" skills/design-review/prompts/intent.md && ! test -f skills/design-review/prompts/copy.md && jq -e '.total_weight == 16' config/scoring.json && jq -e '.weights.copy_quality' config/scoring.json | grep -q "null" && ! jq -e '.weights.copy' config/scoring.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - intent.md contains "copy_quality" in scores object and 4-dimension rubric
    - intent.md `<role>` mentions copy quality assessment
    - copy.md file is deleted
    - scoring.json total_weight is 16
    - scoring.json has no "copy" key in weights
    - scoring.json quick_mode_total_weight remains 13
  </acceptance_criteria>
  <done>Intent specialist has 4 sub-scores (intent_match, originality, ux_flow, copy_quality), scoring.json reflects 16 total weight with no standalone copy, copy.md is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update boss formula and design-review dispatch for 7 specialists</name>
  <read_first>
    - skills/design-review/prompts/boss.md (scoring formula and output format to update)
    - commands/design-review.md (Specialist 7 dispatch to remove, counts to update)
  </read_first>
  <files>
    skills/design-review/prompts/boss.md,
    commands/design-review.md
  </files>
  <action>
  **A. Update boss.md:**

  1. In `<scoring_formula>`: Change full mode formula from:
     `(Intent*3 + Originality*3 + UX_Flow*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Copy + Code) / 17`
     To:
     `(Intent*3 + Originality*3 + UX_Flow*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Code) / 16`
     Remove "Copy" from the formula. Keep the note about Specialist 6 returning three separate scores -- update it to say "Specialist 6 (Intent/Originality/UX) returns four sub-scores including copy_quality. copy_quality is informational and not weighted in the formula."
     Update "10 scored dimensions" to "9 scored dimensions" in the full mode label.

  2. Quick mode formula stays exactly the same (Copy was never in quick mode).

  3. In `<output_format>`:
     - In the human-readable scores table: Remove the `| Copy & Language | {n}/4 | 1x | {one-line} |` row.
     - Change "Specialist 6 returns three scores" to "Specialist 6 returns four sub-scores (Intent, Originality, UX Flow, Copy Quality). Intent, Originality, and UX Flow are weighted. Copy Quality is reported but not weighted."
     - Update the explicit calculation to: `(I*3 + O*3 + UX*2 + T*2 + C*2 + L + Ic + M + Co) / 16 = N/16 = X.XX/4.0`
     - Change "The formula divides by 17, not 15" to "The formula divides by 16"

  4. In the `<boss_output>` JSON example:
     - Remove `"copy": 3` from scores object
     - Add `"copy_quality": 3` under a new key to show it is tracked but separate from weighted scores. Actually -- keep it simpler: include copy_quality in the scores object for data completeness (consumers like the report generator may display it), but make the formula comment clear it is not weighted.
     - Change `"weighted_score"` example to reflect /16 math

  5. In the Requirements section at the bottom: Change "scores: all 10 dimensions (6 in quick mode, null for skipped)" to "scores: all 9 weighted dimensions plus copy_quality (6 in quick mode, null for skipped)".

  **B. Update design-review.md:**

  1. Remove the entire "### Specialist 7: Copy & Language (Claude Haiku Agent)" section (around lines 377-381). This includes the `@` include of copy.md and the "Read the source files" instruction.

  2. Renumber Specialist 8 (Code & Accessibility) to Specialist 7.

  3. In the description/frontmatter at the top (line 2): Change "8 specialist agents + boss synthesizer" to "7 specialist agents + boss synthesizer". Change the dimension list from "font, color, layout, icon, motion, intent, copy, and code" to "font, color, layout, icon, motion, intent/copy, and code".

  4. In Phase 2 heading: Change "Specialist Dispatch (8 Agents in Parallel)" to "Specialist Dispatch (7 Agents in Parallel)". Update "Launch ALL 8 in the SAME turn" to "Launch ALL 7 in the SAME turn".

  5. Quick mode section: Change "4 core specialists (Font, Color, Layout, Intent/Originality)" to keep the same 4 -- no change needed since Copy was never in quick mode. Update "Skip 4, 5, 7, 8" to "Skip 4, 5, 7" (since old 8 is now 7). Update "Saves ~40-50% tokens" text and the 8-specialist references to 7.

  6. In the Reference Files table at the bottom: Remove the copy.md row.

  7. In Edge Cases section: Update "--quick mode" text from "Dispatch Specialists 1, 2, 3, 6 only. Report 'Quick review (4/8 specialists).'" to "Report 'Quick review (4/7 specialists).'"
  </action>
  <verify>
    <automated>cd /Users/felipemachado/Sites/spsk && grep -q "/ 16" skills/design-review/prompts/boss.md && ! grep -q "Copy & Language" commands/design-review.md && grep -q "7 specialist" commands/design-review.md && ! grep -q "prompts/copy.md" commands/design-review.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - boss.md formula divides by 16, not 17
    - boss.md output format has no standalone Copy row
    - boss.md mentions copy_quality as informational (not weighted)
    - design-review.md has no Specialist 7 Copy section
    - design-review.md frontmatter says "7 specialist"
    - design-review.md does not reference prompts/copy.md
    - Quick mode references updated from /8 to /7
  </acceptance_criteria>
  <done>Boss synthesizer uses /16 formula with 9 weighted dimensions, design-review.md dispatches 7 specialists with no Copy section, all count references updated.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -q "copy_quality" skills/design-review/prompts/intent.md` -- intent has 4th sub-score
2. `! test -f skills/design-review/prompts/copy.md` -- copy.md deleted
3. `jq '.total_weight' config/scoring.json` outputs 16
4. `grep "/ 16" skills/design-review/prompts/boss.md` -- boss uses /16
5. `! grep "Copy & Language" commands/design-review.md` -- no Copy dispatch
6. `grep "7 specialist" commands/design-review.md` -- updated count
</verification>

<success_criteria>
The core merge is complete: Intent specialist produces 4 sub-scores including copy_quality, scoring weights sum to 16, boss formula divides by 16, and design-review.md dispatches 7 specialists. The system is functionally correct for a 7-specialist review.
</success_criteria>

<output>
After completion, create `.planning/phases/11-specialist-consolidation/11-01-SUMMARY.md`
</output>
