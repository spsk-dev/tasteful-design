---
phase: 11-specialist-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - commands/design-audit.md
  - commands/design-improve.md
  - shared/output.md
  - evals/validate-structure.sh
  - evals/parse-review-output.sh
  - evals/assertions.json
  - evals/fixtures/report-test/flow-state.json
  - README.md
  - ARCHITECTURE.md
  - CLAUDE.md
autonomous: true
requirements: [SPEC-02, SPEC-03]

must_haves:
  truths:
    - "validate-structure.sh asserts sum of scoring.json weights equals total_weight"
    - "All files referencing 8 specialists now say 7 specialists"
    - "Eval parser maps copy_quality dimension correctly"
    - "Eval assertions recalibrated for /16 weighted score"
    - "validate-structure.sh prompt file loops no longer list copy.md"
  artifacts:
    - path: "evals/validate-structure.sh"
      provides: "Weight-sum assertion and updated prompt file list"
      contains: "weight.*sum"
    - path: "evals/parse-review-output.sh"
      provides: "Updated dimension mapping without standalone copy"
    - path: "evals/assertions.json"
      provides: "Recalibrated score ranges for 16 total weight"
    - path: "README.md"
      provides: "Updated specialist count and formula"
      contains: "7 specialist"
    - path: "ARCHITECTURE.md"
      provides: "Updated specialist count, diagram, and formula"
      contains: "/ 16"
  key_links:
    - from: "evals/validate-structure.sh"
      to: "config/scoring.json"
      via: "weight-sum assertion reads scoring.json and verifies sum"
      pattern: "jq.*weights.*total_weight"
    - from: "evals/parse-review-output.sh"
      to: "skills/design-review/prompts/boss.md"
      via: "dimension key mapping matches boss output schema"
      pattern: "copy_quality"
---

<objective>
Update all ecosystem files to reflect the 7-specialist architecture and add the weight-sum structural assertion.

Purpose: Plan 01 made the core system functional with 7 specialists. This plan ensures every reference file, eval, and doc is consistent -- no stale "8 specialist" counts, no broken eval assertions, and a new structural guard preventing weight drift.

Output: All files across the repo consistently reference 7 specialists, eval assertions recalibrated for /16 scoring, and validate-structure.sh enforces weight sum = total_weight.
</objective>

<execution_context>
@/Users/felipemachado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/felipemachado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-specialist-consolidation/11-CONTEXT.md
@.planning/phases/11-specialist-consolidation/11-01-SUMMARY.md

Key interfaces from Plan 01 (what changed):
- scoring.json: `"copy"` key removed, total_weight is now 16, quick_mode_total_weight stays 13
- copy.md: deleted
- intent.md: now outputs 4 sub-scores including copy_quality
- boss.md: formula is now `/ 16` with 9 weighted dimensions
- design-review.md: 7 specialists, Specialist 7 is now Code & Accessibility

Files to update and what to change:
- commands/design-audit.md: "8 specialists" -> "7 specialists" in all count references
- commands/design-improve.md: "8 specialists" -> "7 specialists", remove "copy" from specialist list
- shared/output.md: specialist_count default from 8 to 7
- evals/validate-structure.sh: remove copy.md from prompt loops, add weight-sum assertion
- evals/parse-review-output.sh: remove "copy" -> "Copy" mapping, add "copy_quality" -> "Copy Quality" mapping
- evals/assertions.json: recalibrate overall score ranges (scores shift slightly with /16 vs /17)
- evals/fixtures/report-test/flow-state.json: rename "copy" key to "copy_quality" in scores
- README.md: "8 specialist" -> "7 specialist", update formula
- ARCHITECTURE.md: "8 specialist" -> "7 specialist", update diagram and formula, remove Copy row
- CLAUDE.md: "8 specialist" -> "7 specialist"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weight-sum assertion and update evals for 7 specialists</name>
  <read_first>
    - evals/validate-structure.sh (add weight-sum assertion, update prompt loops)
    - evals/parse-review-output.sh (update dimension mapping)
    - evals/assertions.json (recalibrate overall score ranges)
    - evals/fixtures/report-test/flow-state.json (rename copy key)
  </read_first>
  <files>
    evals/validate-structure.sh,
    evals/parse-review-output.sh,
    evals/assertions.json,
    evals/fixtures/report-test/flow-state.json
  </files>
  <action>
  **A. validate-structure.sh (per SPEC-03):**

  1. In the prompt file existence loop (line 147), remove `copy.md` from the list:
     Change: `for prompt in font.md color.md layout.md icons.md motion.md intent.md copy.md code-a11y.md boss.md`
     To: `for prompt in font.md color.md layout.md icons.md motion.md intent.md code-a11y.md boss.md`

  2. In the XML tag check loop (line 152), remove `copy.md` from the list:
     Change: `for prompt in font.md color.md layout.md icons.md motion.md intent.md copy.md code-a11y.md`
     To: `for prompt in font.md color.md layout.md icons.md motion.md intent.md code-a11y.md`

  3. Add a NEW weight-sum assertion section AFTER the existing Phase 8 checks (after line 176). Insert:
     ```bash
     # Phase 11: Weight-sum integrity
     WEIGHT_SUM=$(jq '[.weights[]] | add' config/scoring.json)
     TOTAL_WEIGHT=$(jq '.total_weight' config/scoring.json)
     check "Sum of individual weights ($WEIGHT_SUM) equals total_weight ($TOTAL_WEIGHT)" test "$WEIGHT_SUM" -eq "$TOTAL_WEIGHT"
     ```
     This satisfies SPEC-03: "structural assertion in validate-structure.sh verifying sum of weights equals total_weight."

  4. Update the prompt count check if one exists. The existing check at line 59 counts reference files (>= 5). The prompt loops now iterate 8 files (7 specialist + boss) instead of 9. No explicit count assertion exists for prompts, so the loop changes above are sufficient.

  **B. parse-review-output.sh:**

  1. In the `extract_score()` regex fallback case statement (around line 112-124):
     - Remove the line: `copy)          label="Copy" ;;`
     - Add a new line: `copy_quality)  label="Copy Quality" ;;`
     This ensures the parser can extract copy_quality from the boss output table if it falls back to regex.

  **C. assertions.json:**

  The overall score assertions use ranges like min 2.0/max 3.2. With /16 instead of /17, scores shift slightly upward (dividing by a smaller number). The shift is small: for example, a raw sum of 40 gives 40/17=2.35 vs 40/16=2.50 -- about +0.15 shift. But since the "copy" dimension is removed entirely (not just reweighted), the raw sum also decreases by the copy score.

  Net effect: removing a dimension scoring ~2-3 from a /17 divisor and moving to /16 divisor roughly cancels out. The ranges are already broad enough (0.5-1.2 point spreads) to absorb this shift. Keep existing ranges but widen slightly where tight:

  1. "Admin panel gets mid-range overall score": Keep min 2.0, max 3.2 (already wide)
  2. "Landing page scores above admin overall": Keep min 2.5, max 3.5 (already wide)
  3. "Intentionally bad page scores low overall": Keep min 1.0, max 2.0 (already wide)
  4. "Gray-area page scores in CONDITIONAL range": Keep min 1.8, max 2.8 (already wide)

  No changes needed to assertions.json -- existing ranges accommodate the /16 shift. The ranges were designed with 0.3 buffer and LLM stochasticity tolerance.

  However, update the `"version"` field from `"1.1.0"` to `"1.2.0"` to reflect the v1.2.0 milestone changes.

  **D. flow-state.json (report test fixture):**

  1. In all `scores` objects within screens, rename the `"copy"` key to `"copy_quality"`. There are entries at lines 29 and 89 with `"copy": 3.2` and `"copy": 3.0` respectively. Change both to `"copy_quality"`.
  </action>
  <verify>
    <automated>cd /Users/felipemachado/Sites/spsk && bash evals/validate-structure.sh 2>&1 | tail -5 && grep -q "copy_quality" evals/parse-review-output.sh && grep -q "copy_quality" evals/fixtures/report-test/flow-state.json && ! grep -q "copy\.md" evals/validate-structure.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - validate-structure.sh passes (all checks green)
    - validate-structure.sh contains weight-sum assertion checking jq sum equals total_weight
    - validate-structure.sh prompt loops do not include copy.md
    - parse-review-output.sh maps copy_quality dimension, not copy
    - flow-state.json uses copy_quality key, not copy
  </acceptance_criteria>
  <done>Weight-sum structural assertion enforced, eval tooling updated for 7-specialist output, all validate-structure checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update all docs and commands for 7-specialist count</name>
  <read_first>
    - commands/design-audit.md (search for "8 specialist" and "copy" references)
    - commands/design-improve.md (search for "8 specialist" and "copy" references)
    - shared/output.md (specialist_count variable)
    - README.md (specialist count, formula, specialist list)
    - ARCHITECTURE.md (specialist count, diagram, formula, Copy row)
    - CLAUDE.md (specialist count references)
  </read_first>
  <files>
    commands/design-audit.md,
    commands/design-improve.md,
    shared/output.md,
    README.md,
    ARCHITECTURE.md,
    CLAUDE.md
  </files>
  <action>
  **A. commands/design-audit.md:**

  1. All occurrences of "8 specialists" -> "7 specialists". Based on grep results, these are at lines 727, 733, 736, 754, 795.
  2. Line 754: Remove "Copy" from the specialist list: "Font, Color, Layout, Icon, Motion, Intent/Originality/UX, Copy, Code/A11y" -> "Font, Color, Layout, Icon, Motion, Intent/Originality/UX, Code/A11y"
  3. Line 795: `"specialist_count": 8` -> `"specialist_count": 7`
  4. Lines 805, 840: In JSON score objects, rename `"copy"` key to `"copy_quality"`. If `"copy": null` in quick mode, change to `"copy_quality": null`.

  **B. commands/design-improve.md:**

  1. Line 22: "review it with 8 specialists" -> "review it with 7 specialists"
  2. Line 59: Remove "Copy" from specialist list: "Font, Color, Layout, Icon, Motion, Intent, Copy, Code" -> "Font, Color, Layout, Icon, Motion, Intent, Code"
  3. Line 88: Remove "For copy: fix spelling, accents, placeholder text" line (copy issues are now part of Intent specialist findings)
  4. Line 193: "full 8 specialists" -> "full 7 specialists"

  **C. shared/output.md:**

  1. Line 19: Change `**{specialist_count}**: \`8\` for full mode` to `\`7\` for full mode`
  2. Lines 27, 147 (signature line examples): Change "8 specialists" to "7 specialists"
  3. Line 118 (example box): Change "8 specialists" to "7 specialists"

  **D. README.md:**

  1. Line 5: "8 specialist agents" -> "7 specialist agents". Remove "copy" from the dimension list, replace with "intent/copy" or just remove it: "typography, color, layout, icons, motion, intent, and accessibility"
  2. Line 11: alt text "8 specialists" -> "7 specialists"
  3. Line 35: "4 specialists instead of 8" -> "4 specialists instead of 7"
  4. Line 131: Remove the Copy specialist bullet: "- **Copy** (Claude) -- spelling, accents, tone, placeholder text"
  5. Line 136: Update formula: remove "Copy" and change divisor:
     From: `(Intent*3 + Originality*3 + UX*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Copy + Code) / 17`
     To: `(Intent*3 + Originality*3 + UX*2 + Typography*2 + Color*2 + Layout + Icons + Motion + Code) / 16`

  **E. ARCHITECTURE.md:**

  1. Line 7: "8 specialist agents" -> "7 specialist agents"
  2. Line 33: Remove "Copy" from the ASCII diagram specialist list
  3. Line 50: "Specialists 1, 4-8" -> "Specialists 1, 4-7". "Specialists 2 and 3" stays same.
  4. Line 60: Remove the entire Copy row from the specialist table: `| 7 | Copy | Copy Quality | ... | 1x | Claude |`
  5. Line 69: "all 8 specialists" -> "all 7 specialists"
  6. Line 89: Update formula: remove `Copy*1`, change divisor to 16
  7. Line 97: Remove "Copy" from the "1x each" list
  8. Line 114: Update quick mode text: "8" -> "7" in "Runs 4 specialists instead of 8"
  9. Line 173: "8 specialists" -> "7 specialists" in the table
  10. Line 249: "8 specialists" -> "7 specialists" in the flow diagram

  **F. CLAUDE.md:**

  Update all references to "8 specialist" to "7 specialist" and "8 independent specialist" to "7 independent specialist". Based on the file content, these are in the project description at the top and in the command descriptions. Update the specialist list to remove "copy" as standalone and mention "intent/copy" or "intent/originality/UX/copy". Update the scoring formula reference from /17 to /16.
  </action>
  <verify>
    <automated>cd /Users/felipemachado/Sites/spsk && ! grep -rn "8 specialist" commands/design-audit.md commands/design-improve.md shared/output.md README.md ARCHITECTURE.md CLAUDE.md 2>/dev/null | grep -v "4/8\|Tier\|phase\|SKIP\|Phase" && grep -q "7 specialist" README.md && grep -q "/ 16" ARCHITECTURE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - No file in the modified list contains "8 specialist" (except historical/changelog references)
    - README.md formula shows /16 divisor without Copy
    - ARCHITECTURE.md specialist table has no Copy row
    - ARCHITECTURE.md formula shows /16
    - shared/output.md specialist_count default is 7
    - design-audit.md specialist_count is 7 in JSON examples
    - design-improve.md specialist list does not include standalone Copy
  </acceptance_criteria>
  <done>All ecosystem files consistently reference 7 specialists, /16 formula, no standalone Copy specialist. Repository-wide grep for "8 specialist" returns zero results in active files.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bash evals/validate-structure.sh` passes all checks including weight-sum assertion
2. `grep -rn "8 specialist" commands/ shared/ README.md ARCHITECTURE.md CLAUDE.md` returns zero matches in non-historical context
3. `grep "copy_quality" evals/parse-review-output.sh` confirms dimension mapping
4. `grep "copy_quality" evals/fixtures/report-test/flow-state.json` confirms fixture updated
5. `jq '[.weights[]] | add' config/scoring.json` outputs 16
</verification>

<success_criteria>
Every file in the repository consistently reflects the 7-specialist architecture. The weight-sum assertion in validate-structure.sh will catch any future drift between individual weights and total_weight. Eval tooling correctly parses the new copy_quality dimension from boss output.
</success_criteria>

<output>
After completion, create `.planning/phases/11-specialist-consolidation/11-02-SUMMARY.md`
</output>
